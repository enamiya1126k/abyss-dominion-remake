export function unlock539(u){if(!u.sound)return;try{const Audio=globalThis.AudioContext??globalThis.webkitAudioContext;if(!Audio)return;u.audio??=new Audio();u.audio.resume()?.catch(()=>{})}catch{}}
export function sound539(u,type){if(!u.sound||u.audio?.state!=='running')return;const a=u.audio,notes={begin:[330,440,660],jump:[270,520],land:[640],splash:[180,95],wave:[130,110,90],checkpoint:[440,660,880],goal:[523,659,784,1046],finish:[523,659,784,1046]}[type];if(!notes)return;notes.forEach((n,i)=>{const o=a.createOscillator(),v=a.createGain(),t=a.currentTime+i*.075;o.type=type==='splash'||type==='wave'?'triangle':'sine';o.frequency.setValueAtTime(n,t);if(type==='jump')o.frequency.exponentialRampToValueAtTime(n*1.4,t+.13);v.gain.setValueAtTime(0,t);v.gain.linearRampToValueAtTime(.038,t+.008);v.gain.exponentialRampToValueAtTime(.001,t+.19);o.connect(v);v.connect(a.destination);o.start(t);o.stop(t+.21)})}
