import{CARDS463,CARD_BY_ID463,SPECIAL_BY_ID463,TIMING463,DECK_COUNT463}from'./Catalog463.js';
import{BOARD463,NODES463,BOARD_SIZE463,ATTRS463,distance463}from'./Board463.js';
import{partySeats462}from'../party/PartyView462.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const colors=['#68b4ff','#79dda9','#ffb367','#ef91ca'];
const ui=c=>c.sgUI463??=( {filter:'all',sort:'received',follow:true,zoom:.8,x:0,y:0,modal:null,scrolls:{}} );
export const art463=(n,cls='')=>`<span class="sg-art ${cls}" style="background-position:${(n%4)*100/3}% ${Math.floor(n/4)*100/3}%" aria-hidden="true"></span>`;
const cardArt=c=>c.set==='dark'?`<span class="sg-art" style="background-size:800% 800%;background-position:${(2+c.part%2)*100/7}% ${(4+Math.floor(c.part/2))*100/7}%" aria-hidden="true"></span>`:art463(c.art);
const attr=c=>c.attrs.length===4?'全属性':c.attrs.length?c.attrs.map(a=>ATTRS463.find(x=>x.id===a)?.name).join('・'):'無属性';
const token=(c,p)=>c.sgMonster463?.(p.choice?.speciesId)??art463(12+(p.seat??0)%4);
const button=(action,text,disabled=false,cls='')=>`<button class="${cls}" data-sg-action="${action}" ${disabled?'disabled':''}>${text}</button>`;
function cardFace(card,{uid='',small=false,usable=false,ward=false,action='card'}={}){return`<button class="sg-card sg-${card.kind}${usable?' is-usable':''}${small?' sg-card-small':''}" data-sg-action="${action}" data-card="${esc(card.id)}" data-uid="${esc(uid)}"><span class="sg-card-top"><span>${esc(TIMING463[card.timing])}</span><b>${card.points>0?'+':''}${card.points}<small>pt</small></b></span>${cardArt(card)}<strong>${esc(card.name)}</strong><span class="sg-attrs">${card.attrs.length?card.attrs.map(a=>`<i style="--attr:${ATTRS463.find(x=>x.id===a)?.color}">${esc(ATTRS463.find(x=>x.id===a)?.name)}</i>`).join(''):'<i class="sg-neutral">無</i>'}</span><span class="sg-card-caption">${ward?'防御札に変化中':usable?'タップして使う':card.kind==='curse'?'相手に渡せる呪い':card.set?'4種類を集めよう':esc(card.text)}</span></button>`}
function specialFace(id){const s=SPECIAL_BY_ID463[id];return s?`<div class="sg-special-detail">${art463(s.art)}<div><small>特殊カード · 公開</small><h2>${esc(s.name)} <b>${s.points>0?'+':''}${s.points}pt</b></h2><p>${esc(s.text)}</p></div></div>`:'<p>特殊カードはまだありません。</p>'}
function header(c,g){return`<header class="sg-header">${button('browse','‹ パーティー')}<div><small>ABYSS DOMINION</small><b>カードすごろく</b></div>${button('rules','遊び方')}</header>${!c.connected()?'<div class="sg-notice" role="status">再接続中… 手札と盤面を保持しています。</div>':''}${c.error?`<div class="sg-notice sg-error" role="alert">${esc(c.error)} ${button('refresh','更新')}</div>`:''}`}
function lobby(c,g){const p=c.state.party,id=c.transport.selfId,me=g.members.find(m=>m.playerId===id),ready=p.members.find(m=>m.playerId===id)?.ready,host=p.hostId===id,roster=c.roster(),q=ui(c).search??'',rows=roster.filter(m=>(c.sgSpeciesName463?.(m.speciesId)??m.speciesId).includes(q)),limit=ui(c).rosterLimit??24;return`<section class="sg-screen sg-lobby">${header(c,g)}<div class="sg-lobby-hero"><img src="./assets/sugoroku463/cover.png" alt="魔物たちとカードが集う星の回廊"><div><span>星砕きの回廊</span><h1>運命は、手札の中に。</h1><p>先にゴールするか。最後まで切り札を隠すか。</p><b>4人対戦 · 足りない席はAI · 目安20〜40分</b></div></div><div class="sg-lobby-body"><div class="sg-room-code"><span>合言葉 <b>${esc(p.code)}</b></span><button data-party-action462="copy">コピー</button><button data-party-action462="invite">招待リンク</button></div>${partySeats462(c)}<div class="sg-lobby-summary"><span><b>${CARDS463.length}種類</b>山札${DECK_COUNT463}枚</span><span><b>16種類</b>公開の特殊カード</span><span><b>3つの分岐</b>カードが集まる寄り道</span></div><div class="sg-section-line"><h2>コマにする魔物を選ぶ</h2><small>強さによる有利・不利はありません</small></div><input name="sgSearch463" type="search" placeholder="魔物の名前で探す" value="${esc(q)}" aria-label="コマにする魔物を検索"><div class="sg-roster" data-sg-scroll="roster">${rows.slice(0,limit).map(m=>`<button data-sg-action="select" data-monster="${esc(m.id)}" class="${me?.choice?.id===m.id?'is-selected':''}">${token(c,{choice:m})}<b>${esc(c.sgSpeciesName463?.(m.speciesId)??m.speciesId)}</b><small>${me?.choice?.id===m.id?'✓ 選択中':'コマにする'}</small></button>`).join('')||'<p>該当する魔物がいません。</p>'}</div>${rows.length>limit?button('more','さらに表示'):''}<div class="sg-lobby-actions"><button data-party-action462="ready" aria-pressed="${!!ready}" ${!me?.choice||!c.ready()?'disabled':''}>${ready?'✓ 準備OK':'準備OK'}</button>${host?button('start','冒険をはじめる',!c.ready()||p.members.some(m=>!m.ready||!m.connected||m.atHome),'sg-primary'):'<p>全員の準備ができたら、部屋主が開始します。</p>'}</div><p class="sg-muted">3人目のゴールで終了。手札＋特殊カード＋到着ボーナスの合計点で勝負！</p></div>${modal(c,g)}</section>`}
function board(c,g){const u=ui(c);return`<div class="sg-board-wrap"><div class="sg-board-tools"><span>星砕きの回廊 <small>マスをタップで説明</small></span><div>${button('zoomOut','−',false,'sg-icon')}${button('zoomIn','＋',false,'sg-icon')}${button('overview','全体')}${button('follow',u.follow?'追尾中':'手番へ',false,u.follow?'is-active':'')}</div></div><div class="sg-viewport" aria-label="すごろく盤。ドラッグで移動、ピンチで拡大"><div class="sg-world" style="width:${BOARD_SIZE463.width}px;height:${BOARD_SIZE463.height}px"><svg class="sg-map" viewBox="0 0 ${BOARD_SIZE463.width} ${BOARD_SIZE463.height}" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="sgPath463"><stop stop-color="#6d5185"/><stop offset="1" stop-color="#dbb975"/></linearGradient></defs><g class="sg-paths">${BOARD463.flatMap(n=>n.next.map(id=>{const end=NODES463[id];return`<path d="M${n.x} ${n.y} L${end.x} ${end.y}" class="${n.detour||end.detour?'sg-detour-path':''}"/>`})).join('')}</g>${BOARD463.map(n=>`<g class="sg-tile sg-tone-${n.tone}" transform="translate(${n.x},${n.y})" role="button" tabindex="0" aria-label="${esc(n.name)}。${esc(n.text)}" data-sg-tile="${n.id}"><rect x="-53" y="-45" width="106" height="90" rx="17"/><text class="sg-tile-number" x="-40" y="-25">${n.detour?'寄り道':n.index===0?'START':n.index===79?'GOAL':n.index}</text><text class="sg-tile-icon" x="0" y="9">${n.icon}</text><text class="sg-tile-name" x="0" y="32">${esc(n.name)}</text>${n.fork?'<circle cx="46" cy="-33" r="12" class="sg-fork-dot"/><text x="46" y="-29" class="sg-fork-label">分</text>':''}</g>`).join('')}</svg>${g.players.map(p=>{const n=NODES463[p.pos],stack=g.players.filter(x=>x.pos===p.pos),i=stack.indexOf(p),x=n.x+(i-(stack.length-1)/2)*25,y=n.y-40;return`<div class="sg-token${p.playerId===g.turnPlayerId?' is-current':''}${p.finished?' is-finished':''}" data-sg-token="${esc(p.playerId)}" style="left:${x}px;top:${y}px;--seat:${colors[p.seat]}">${token(c,p)}<b>${esc(p.name)}${p.finished?' · '+p.finished.place+'着':''}</b></div>`}).join('')}</div><div class="sg-board-vignette"></div><div class="sg-roll-display" aria-live="polite">${g.lastRoll?`<small>${esc(g.players.find(p=>p.playerId===g.lastRoll.playerId)?.name)}の出目</small><div>${g.lastRoll.dice.map(n=>`<i>${['⚀','⚁','⚂','⚃','⚄','⚅'][n-1]}</i>`).join('')}<b>${g.lastRoll.total}<small>マス</small></b></div>`:'<small>カードを引いて、冒険のはじまり。</small>'}</div></div><div class="sg-ticker" aria-live="polite"><span>✦</span>${esc(g.log.at(-1)?.text??'冒険の準備中')} ${button('log','履歴')}</div></div>`}
function seats(c,g){return`<div class="sg-seats">${g.players.map(p=>`<button data-sg-action="player" data-player="${esc(p.playerId)}" class="sg-seat${p.playerId===g.turnPlayerId&&!p.finished?' is-turn':''}${p.playerId===c.transport.selfId?' is-self':''}" style="--seat:${colors[p.seat]}"><span class="sg-seat-name">${p.ai?'AI ':''}${esc(p.name)}</span><b>${p.finished?p.finished.place+'着 · '+p.finished.total+'点':'残り'+distance463(p.pos)+'マス'}</b><small>手札 ${p.handCount}枚${p.skip?' · 休'+p.skip:''}</small><em>${p.special?esc(SPECIAL_BY_ID463[p.special]?.name):'特殊なし'}</em></button>`).join('')}</div>`}
function hand(c,g){const u=ui(c),own=g.players.find(p=>p.playerId===c.transport.selfId);if(!own)return'<div class="sg-observer">今回は観戦中。次のゲームから参加できます。</div>';let cards=g.hand.map(h=>({...h,c:CARD_BY_ID463[h.cardId]}));if(u.filter==='play')cards=cards.filter(h=>h.playable);else if(u.filter==='curse')cards=cards.filter(h=>h.c.kind==='curse');else if(u.filter!=='all')cards=cards.filter(h=>h.c.attrs.includes(u.filter));if(u.sort==='points')cards.sort((a,b)=>b.c.points-a.c.points);const sets=['heroes','dark'].map(set=>({set,n:new Set(g.hand.map(h=>CARD_BY_ID463[h.cardId]).filter(c=>c.set===set).map(c=>c.part)).size})).filter(x=>x.n);
return`<section class="sg-hand-section"><div class="sg-hand-heading"><h2>あなたの手札 <b>${g.hand.length}</b></h2><span>現在 ${g.ownScore?.total??0}pt <small>＋到着ボーナス</small></span><select name="sgSort463" aria-label="手札の並び順"><option value="received" ${u.sort==='received'?'selected':''}>入手順</option><option value="points" ${u.sort==='points'?'selected':''}>得点順</option></select></div><div class="sg-filters" data-sg-scroll="filters">${[['all','すべて'],['play','使える'],...ATTRS463.map(a=>[a.id,a.name]),['curse','呪い']].map(([id,name])=>`<button data-sg-action="filter" data-filter="${id}" class="${u.filter===id?'is-active':''}" aria-pressed="${u.filter===id}">${name}</button>`).join('')}${sets.map(x=>`<span class="sg-set-progress ${x.n===4?'is-complete':''}">${x.set==='heroes'?'四勇者':'闇の絵柄'} ${x.n}/4</span>`).join('')}</div><div class="sg-hand" data-sg-scroll="hand">${cards.map(h=>cardFace(h.c,{uid:h.uid,usable:h.playable,ward:h.ward,small:true})).join('')||'<p class="sg-hand-empty">この条件のカードはありません。</p>'}</div></section>`}
function controls(c,g){const me=g.players.find(p=>p.playerId===c.transport.selfId),turn=g.turnPlayerId===c.transport.selfId,actor=g.players.find(p=>p.playerId===g.turnPlayerId),q=g.pending,disabled=!c.connected()||!!ui(c).inFlight;let text=me?.finished?'ゴール済み · 得点確定':!me?'観戦中':turn?g.step==='draw'?'まずカードを1枚引こう':g.step==='pre'?'移動前のカードを使えます':'移動後のカードを使えます':`${actor?.name??'相手'}の手番`;if(q)text=q.playerId===c.transport.selfId?'あなたの選択を待っています':`${g.players.find(p=>p.playerId===q.playerId)?.name??'相手'}が選択中`;
 const specials=['chuni','surprise','punch','shiva'].includes(me?.special)&&!g.ownSpecialUsed;
 return`<div class="sg-action-dock"><div class="sg-turn-label"><i class="${turn?'is-you':''}"></i><b>${esc(text)}</b><small data-sg-timer></small></div><div class="sg-action-buttons">${q?.playerId===c.transport.selfId?button('choiceOpen','選択を開く',disabled,'sg-primary'):!q&&turn&&!me?.finished?`${g.step==='draw'?button('draw','カードを引く',disabled,'sg-primary'):g.step==='pre'?button('roll',g.ownMods?.noRoll?'移動を確定する':'サイコロを振る',disabled,'sg-primary'):button('end','手番を終える',disabled,'sg-primary')}${specials&&g.step!=='draw'?button('special','特殊能力',disabled):''}`:'<span class="sg-waiting">手札やマスを見ながら作戦を考えよう。</span>'}${button('mine',me?.special?esc(SPECIAL_BY_ID463[me.special].name):'特殊カード')}</div></div>`}
function results(c,g){const host=c.state.party?.hostId===c.transport.selfId;return`<section class="sg-screen sg-result">${header(c,g)}<div class="sg-result-hero"><small>星砕きの回廊 · 結果</small><h1>${esc(g.results[0].name)}</h1><p>得点1位 <b>${g.results[0].total}<small>pt</small></b></p>${token(c,g.players.find(p=>p.playerId===g.results[0].playerId))}</div><div class="sg-results-table" data-sg-scroll="results"><table><thead><tr><th>順位 / プレイヤー</th><th>到着</th><th>手札</th><th>特殊</th><th>加点</th><th>合計</th></tr></thead><tbody>${g.results.map((p,i)=>`<tr class="${p.playerId===c.transport.selfId?'is-self':''}"><th><button data-sg-action="player" data-player="${esc(p.playerId)}"><b>${i+1}</b> ${esc(p.name)}</button></th><td>${p.place}着<br><small>＋${p.bonus}</small></td><td>${p.hand}</td><td>${p.special}</td><td>${p.affinity}</td><td><b>${p.total}</b></td></tr>`).join('')}</tbody></table></div><p class="sg-muted">得点が同じ場合は、先に到着した人が上位。<br>プレイヤー名から、最後の手札を確認できます。</p><div class="sg-result-actions">${host?button('again','同じメンバーでもう一度',!c.connected(),'sg-primary'):button('browse','パーティーに戻る',false,'sg-primary')}${host?'<button data-party-action462="lounge">別のゲームを選ぶ</button>':''}<button data-race-action="back">ホームへ戻る</button></div>${modal(c,g)}</section>`}
function rules(){return`<div class="sg-rules"><h2>手札を残して、得点を競う。</h2><p>手札の点数＋特殊カードの点数＋到着ボーナスが最終得点。3人目がゴールした時点で終了し、残る1人を4着として精算します。</p><div class="sg-bonus-row"><b>1着 ＋40</b><b>2着 ＋20</b><b>3着 ＋10</b><b>4着 ＋5</b></div><h3>自分の手番</h3><ol><li>カードを1枚引く。速攻はすぐに発動。</li><li>移動前のカードを好きなだけ使う。</li><li>サイコロを振り、進んだマスのイベントを処理。</li><li>移動後のカードや特殊能力を使って、手番を終える。</li></ol><p>「合計出目＋2」は2マス分加算。「サイコロ＋2個」は通常の1個と合わせて3個を振ります（最大5個）。</p><h3>カードと属性</h3><p>最初の手札は速攻以外の3枚。枚数制限なし。手札は自分だけが見られます。青＝より、緑＝りおん、橙＝えなみ、桃＝ひで。複数属性はそれぞれに該当し、全属性は4属性、無属性はそのどれにも該当しません。</p><p>カードを使うと捨て札へ。捨て札を回収したカードは同じ手番では再使用できません。絵柄4種は隠して温存し、自分の移動前に任意発動。各セットにつき1人1回です。</p><h3>妨害と防御</h3><p>「自分のターン」は相手の手番に使えません。ただし攻撃や悪いマスへの防御は割り込めます。防御はそのカード1枚・マス1つによる自分への残りの効果を無効化。反射は1回だけで連鎖しません。</p><p>呪いは持っている間ずっと影響します。相手に手札を引かせると、呪いも渡せます。ハッカーは速攻を手札に隠せるので、さらに危険なババ抜きに！</p><h3>移動とゴール</h3><p>追加移動した先のマスは原則発動しません。「マス効果も発動」と書いたカードだけは例外です。「必ず止まる」門は前進中に必ず止まり、イベントを行います。</p><p>ゴールは出目が超えてもOK。到着した人の手番と得点は確定し、以後は攻撃・交換の対象外になります。特殊カードは公開で1枚だけ。新たに得たら残す1枚を選べます。</p><h3>待ち時間と長期戦</h3><p>選択に75秒かかるとAIが一時代行。切断中は30秒後から代行し、接続が戻れば操作できます。85手番を超える長期戦では少しずつ移動を加速します。追加ターンは連続2手番までです。</p><p>このゲームのカード・点数は対戦専用です。育成や所持GOLDを使わずに遊べます。</p></div>`}
function modal(c,g){const u=ui(c),q=g.pending?.playerId===c.transport.selfId?g.pending:null,m=u.modal??(q&&u.dismissChoice!==q.id?{kind:'choice'}:null);if(!m)return'';let body='',title='',close=true;
 if(m.kind==='rules'){title='遊び方';body=rules()}
 if(m.kind==='tile'){const n=NODES463[m.id];title=n.name;body=`<div class="sg-tile-detail sg-tone-${n.tone}"><b>${n.icon}</b><p>${esc(n.text)}</p>${n.fork?'<p>ここから分岐。近道と、カードを集める寄り道を選べます。</p>':''}</div>`}
 if(m.kind==='card'){const card=CARD_BY_ID463[m.id],owned=g.hand?.find(h=>h.uid===m.uid),isSet=!!card.set,parts=isSet?new Set(g.hand.map(h=>CARD_BY_ID463[h.cardId]).filter(x=>x.set===card.set).map(x=>x.part)).size:0;title=card.name;body=`<div class="sg-inspect">${cardArt(card)}<div class="sg-inspect-meta"><b>${card.points>0?'+':''}${card.points}pt</b><span>${esc(attr(card))} · ${TIMING463[card.timing]}</span></div><p>${esc(card.text)}</p>${isSet?`<p>そろった絵柄：${parts}/4種類。発動すると4枚を消費。各セットにつき1人1回。</p>`:''}${owned?.playable?`<button class="sg-primary" data-sg-action="play" data-uid="${esc(m.uid)}" ${!c.connected()||u.inFlight?'disabled':''}>${isSet?'4枚を使って切り札を発動':'このカードを使う'}</button>`:`<div class="sg-muted">${owned?.ward?'自分への攻撃時に防御として使えます。':card.timing==='reaction'?'攻撃を受けた時に、防御として選べます。':!owned?'カードの効果を確認中':card.timing==='passive'?'持っている間、効果が適用されます。':'今は使えるタイミングではありません。'}</div>`}</div>`}
 if(m.kind==='special'){title='公開の特殊カード';body=specialFace(m.id)}
 if(m.kind==='player'){const p=g.players.find(x=>x.playerId===m.id);title=p.name;body=`${specialFace(p.special)}<p>手札 ${p.handCount}枚 / ${p.finished?p.finished.place+'着・'+p.finished.total+'点確定':'ゴールまで残り'+distance463(p.pos)+'マス'}</p>${p.finished?`<div class="sg-card-grid">${p.finished.handCards.map(id=>cardFace(CARD_BY_ID463[id],{small:true})).join('')}</div>`:p.playerId===c.transport.selfId?'<p>自分の手札は画面下にあります。</p>':'<p class="sg-muted">相手の手札の中身は秘密です。</p>'}`}
 if(m.kind==='log'){title='冒険の記録';body=`<div class="sg-log">${[...g.log].reverse().map(e=>`<p><small>${e.turn}手番</small>${esc(e.text)}${e.cardId?`<button data-sg-action="card" data-card="${e.cardId}">カードを見る</button>`:''}</p>`).join('')}</div><p class="sg-muted">山札 ${g.deckCount}枚 / 捨て札 ${g.discardCount}枚</p>${button('discard','捨て札を見る')}`}
 if(m.kind==='discard'){title=`捨て札 ${g.discardCount}枚`;body=`<div class="sg-card-grid">${[...(g.discardCards??[])].reverse().map(id=>cardFace(CARD_BY_ID463[id],{small:true})).join('')}</div>`}
 if(m.kind==='choice'&&q){title=q.prompt;body=`<p class="sg-muted">${q.kind==='defense'?'防御するか、そのまま受けるか選んでください。':q.kind==='steal'?'中身は秘密。1枚選んで引こう。':'選んだ内容で処理を進めます。'}</p><div class="sg-choices ${q.kind==='steal'?'sg-concealed':''}" data-sg-scroll="choices">${q.options.map(o=>`<div><button data-sg-action="choice" data-value="${esc(o.value)}" ${!c.connected()||u.inFlight?'disabled':''}>${o.special?art463(SPECIAL_BY_ID463[o.special].art):o.cardId?art463(CARD_BY_ID463[o.cardId].art):q.kind==='steal'?'<span class="sg-card-back">✦</span>':''}<b>${esc(o.label)}</b>${o.cardId?`<small>${CARD_BY_ID463[o.cardId].points}pt · ${esc(attr(CARD_BY_ID463[o.cardId]))}</small>`:''}${o.special?`<p>${esc(SPECIAL_BY_ID463[o.special].text)}</p><small>${SPECIAL_BY_ID463[o.special].points}pt</small>`:''}${o.note?`<small>${esc(o.note)}</small>`:''}</button>${o.cardId?`<button class="sg-choice-inspect" data-sg-action="card" data-card="${o.cardId}">効果を読む</button>`:''}</div>`).join('')}</div>`}
 if(!body)return'';return`<div class="sg-modal-backdrop"><section class="sg-modal" role="dialog" aria-modal="true" aria-label="${esc(title)}"><header><b>${esc(title)}</b>${button('close','閉じる ×')}</header><div class="sg-modal-body" data-sg-scroll="modal">${body}</div></section></div>`}
export function sugorokuView463(c){const g=c.state.sugoroku;if(!g)return'';const u=ui(c);if(u.gameId!==g.id){u.gameId=g.id;u.follow=true;u.cameraKey=null;u.modal=null;u.scrolls={};u.inFlight=null}if(g.phase==='lobby')return lobby(c,g);if(g.phase==='result')return results(c,g);return`<section class="sg-screen sg-game">${header(c,g)}${seats(c,g)}${board(c,g)}${controls(c,g)}${hand(c,g)}${modal(c,g)}</section>`}
export function sugorokuBefore463(c){const u=ui(c);for(const el of c.root?.querySelectorAll('[data-sg-scroll]')??[])u.scrolls[el.dataset.sgScroll]={x:el.scrollLeft,y:el.scrollTop};u.positions={};for(const el of c.root?.querySelectorAll('[data-sg-token]')??[])u.positions[el.dataset.sgToken]={left:el.style.left,top:el.style.top};u.cleanup?.();u.cleanup=null;u.dragging=false}
function send(c,payload){const g=c.state.sugoroku,u=ui(c);if(!c.connected()||u.inFlight&&Date.now()-u.inFlight<4000)return;u.inFlight=Date.now();c.error='';u.modal=null;const requestId=(globalThis.crypto?.randomUUID?.()??`${Date.now()}_${Math.random().toString(36).slice(2)}`).replace(/[^a-zA-Z0-9_-]/g,'');c.raw('sg463',{rulesVersion:9,gameId:g.id,revision:g.revision,choiceId:g.pending?.id,requestId,...payload});c.render()}
export function sugorokuClick463(c,b){if(!b.dataset.sgAction)return false;const a=b.dataset.sgAction,u=ui(c),g=c.state?.sugoroku;if(!g)return true;
 if(a==='browse'){c.partyBrowse462=true;c.render();return true}if(a==='refresh'){c.refresh();return true}
 if(a==='zoomIn'||a==='zoomOut'||a==='overview'||a==='follow'){cameraButton(c,a);return true}
 if(a==='select'){send(c,{kind:'select',monsterId:b.dataset.monster,roster:c.roster()});return true}
 if(['start','draw','roll','end','special'].includes(a)){send(c,{kind:a});return true}
 if(a==='play'){send(c,{kind:'play',uid:b.dataset.uid});return true}
 if(a==='choice'){send(c,{kind:'choice',value:b.dataset.value});return true}
 if(a==='again'){c.raw('partyGame462',{game:'sugoroku'});return true}
 if(a==='filter')u.filter=b.dataset.filter;else if(a==='more')u.rosterLimit=(u.rosterLimit??24)+24;
 else if(a==='card'){u.modal={kind:'card',id:b.dataset.card,uid:b.dataset.uid};u.scrolls.modal={x:0,y:0}}
 else if(a==='mine')u.modal={kind:'special',id:g.players.find(p=>p.playerId===c.transport.selfId)?.special};else if(a==='player')u.modal={kind:'player',id:b.dataset.player};
 else if(a==='close'){if(u.modal?.kind==='card'&&g.pending?.playerId===c.transport.selfId){u.modal={kind:'choice'};u.dismissChoice=null}else{u.modal=null;u.dismissChoice=g.pending?.id}}
 else if(a==='choiceOpen'){u.dismissChoice=null;u.modal={kind:'choice'}}else if(['rules','log','discard'].includes(a))u.modal={kind:a};c.render();return true;
}
export function sugorokuInput463(c,e){if(e.name==='sgSort463'){ui(c).sort=e.value;c.render();return true}if(e.name==='sgSearch463'){ui(c).search=e.value;ui(c).rosterLimit=24;c.render();return true}return false}
function applyCamera(c){const u=ui(c),el=c.root?.querySelector('.sg-world'),v=el?.parentElement;if(v){const w=BOARD_SIZE463.width*u.zoom,h=BOARD_SIZE463.height*u.zoom;u.x=w<v.clientWidth?(v.clientWidth-w)/2:Math.min(0,Math.max(v.clientWidth-w,u.x));u.y=h<v.clientHeight?(v.clientHeight-h)/2:Math.min(0,Math.max(v.clientHeight-h,u.y))}if(el)el.style.transform=`translate(${u.x}px,${u.y}px) scale(${u.zoom})`}
function follow(c){const u=ui(c),g=c.state.sugoroku,p=g.players.find(p=>p.playerId===g.turnPlayerId),n=NODES463[p?.pos],v=c.root.querySelector('.sg-viewport');if(!n||!v)return;u.x=v.clientWidth/2-n.x*u.zoom;u.y=v.clientHeight*.5-n.y*u.zoom;applyCamera(c)}
function zoom(c,z,cx,cy){const u=ui(c),v=c.root.querySelector('.sg-viewport');if(!v)return;cx??=v.clientWidth/2;cy??=v.clientHeight/2;z=Math.max(.16,Math.min(1.6,z));u.x=cx-(cx-u.x)*z/u.zoom;u.y=cy-(cy-u.y)*z/u.zoom;u.zoom=z;applyCamera(c)}
function cameraButton(c,a){const u=ui(c),v=c.root.querySelector('.sg-viewport');if(!v)return;if(a==='follow'){u.follow=true;u.zoom=.8;follow(c)}else if(a==='overview'){u.follow=false;u.zoom=Math.min(v.clientWidth/BOARD_SIZE463.width,v.clientHeight/BOARD_SIZE463.height)*.96;u.x=(v.clientWidth-BOARD_SIZE463.width*u.zoom)/2;u.y=(v.clientHeight-BOARD_SIZE463.height*u.zoom)/2;applyCamera(c)}else{u.follow=false;zoom(c,u.zoom*(a==='zoomIn'?1.25:.8))}const b=c.root.querySelector('[data-sg-action="follow"]');b?.classList.toggle('is-active',u.follow);if(b)b.textContent=u.follow?'追尾中':'手番へ'}
export function sugorokuAfter463(c){const g=c.state?.sugoroku,u=ui(c);if(!g||c.partyBrowse462)return;for(const el of c.root.querySelectorAll('[data-sg-scroll]')){const s=u.scrolls[el.dataset.sgScroll];if(s){el.scrollLeft=s.x;el.scrollTop=s.y}}
 if(u.lastRevision!==g.revision){u.inFlight=null;u.lastRevision=g.revision}const v=c.root.querySelector('.sg-viewport');if(v){const p=g.players.find(p=>p.playerId===g.turnPlayerId),key=`${g.turnPlayerId}:${p?.pos}`;if(u.follow&&key!==u.cameraKey){follow(c);u.cameraKey=key}else applyCamera(c);
 const points=new Map();let last=null,moved=false,start=null;
 const down=e=>{if(e.button>0)return;points.set(e.pointerId,{x:e.clientX,y:e.clientY});v.setPointerCapture?.(e.pointerId);last=measure(points);start={x:e.clientX,y:e.clientY};moved=false;u.dragging=true};
 const motion=e=>{if(!points.has(e.pointerId))return;points.set(e.pointerId,{x:e.clientX,y:e.clientY});const next=measure(points);if(last){if(Math.hypot(e.clientX-(start?.x??0),e.clientY-(start?.y??0))>5)moved=true;u.x+=next.x-last.x;u.y+=next.y-last.y;if(next.d&&last.d){const r=v.getBoundingClientRect();zoom(c,u.zoom*next.d/last.d,next.x-r.left,next.y-r.top)}if(moved)u.follow=false;applyCamera(c)}last=next};
 const up=e=>{points.delete(e.pointerId);last=measure(points);if(!points.size){u.dragging=false;if(!moved){const tile=document.elementFromPoint(e.clientX,e.clientY)?.closest('[data-sg-tile]');if(tile){u.modal={kind:'tile',id:tile.dataset.sgTile};u.scrolls.modal={x:0,y:0}}}c.render()}};
 const wheel=e=>{e.preventDefault();u.follow=false;const r=v.getBoundingClientRect();zoom(c,u.zoom*Math.exp(-e.deltaY*.0015),e.clientX-r.left,e.clientY-r.top)};
 v.addEventListener('pointerdown',down);v.addEventListener('pointermove',motion);v.addEventListener('pointerup',up);v.addEventListener('pointercancel',up);v.addEventListener('wheel',wheel,{passive:false});u.cleanup=()=>{v.removeEventListener('pointerdown',down);v.removeEventListener('pointermove',motion);v.removeEventListener('pointerup',up);v.removeEventListener('pointercancel',up);v.removeEventListener('wheel',wheel)};
 if(!matchMedia('(prefers-reduced-motion: reduce)').matches)for(const el of c.root.querySelectorAll('[data-sg-token]')){const old=u.positions[el.dataset.sgToken];if(old&&(old.left!==el.style.left||old.top!==el.style.top))el.animate([{left:old.left,top:old.top},{left:el.style.left,top:el.style.top}],{duration:700,easing:'ease-in-out'})}
 }
 if(u.animationRevision!==g.revision){const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
  if(g.phase==='result'&&u.previousPhase!=='result')c.sound452?.play(g.results?.[0]?.playerId===c.transport.selfId?'win':'finish');
  if(g.lastRoll&&g.lastRoll.eventId!==u.rollEvent){u.rollEvent=g.lastRoll.eventId;c.sound452?.play('count');if(!reduced)c.root.querySelector('.sg-roll-display')?.animate([{transform:'translateY(10px) scale(.75)',opacity:.3},{transform:'translateY(0) scale(1)',opacity:1}],{duration:420,easing:'ease-out'})}
  const reveal=[...(g.log??[])].reverse().find(e=>e.cardId&&e.id>(u.lastEvent??0));if(reveal&&u.lastEvent!=null&&v&&!reduced){const card=CARD_BY_ID463[reveal.cardId];if(card){const el=document.createElement('div');el.className='sg-card-flash';el.innerHTML=art463(card.art)+'<div><small>カード発動</small><b>'+esc(card.name)+'</b></div>';v.append(el);setTimeout(()=>el.remove(),1500);c.sound452?.play(card.kind==='rare'||card.kind==='set'?'spurt':'reveal')}}
  if(!reduced&&u.previousHand)for(const b of c.root.querySelectorAll('.sg-hand [data-uid]'))if(!u.previousHand.has(b.dataset.uid))b.animate([{transform:'translateY(22px) rotateY(75deg)',opacity:.1},{transform:'translateY(0) rotateY(0)',opacity:1}],{duration:420,easing:'ease-out'});
  u.previousHand=new Set((g.hand??[]).map(h=>h.uid));u.lastEvent=g.log?.at(-1)?.id;u.previousPhase=g.phase;u.animationRevision=g.revision;
 }
 const dialog=c.root.querySelector('.sg-modal');if(dialog&&!dialog.contains(document.activeElement))dialog.querySelector('button')?.focus({preventScroll:true});
}
function measure(points){const a=[...points.values()];if(!a.length)return null;return{x:a.reduce((n,p)=>n+p.x,0)/a.length,y:a.reduce((n,p)=>n+p.y,0)/a.length,d:a.length>1?Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y):0}}
export function sugorokuTick463(c){const el=c.root?.querySelector('[data-sg-timer]'),g=c.state?.sugoroku;if(el&&g){const s=Math.max(0,Math.ceil((g.deadline-Date.now()-c.offset)/1000));el.textContent=s?`${s}秒`:'処理中'}if(ui(c).inFlight&&Date.now()-ui(c).inFlight>5000){ui(c).inFlight=null;c.refresh();c.render()}}
export function sugorokuKey463(c,e){if(e.key==='Escape'&&c.root.querySelector('.sg-modal')){e.preventDefault();c.root.querySelector('[data-sg-action="close"]')?.click();return true}const tile=e.target.closest('[data-sg-tile]');if(tile&&['Enter',' '].includes(e.key)){e.preventDefault();ui(c).modal={kind:'tile',id:tile.dataset.sgTile};c.render();return true}const dialog=c.root.querySelector('.sg-modal');if(dialog&&e.key==='Tab'){const nodes=[...dialog.querySelectorAll('button,select,input,[tabindex="0"]')].filter(e=>!e.disabled);const first=nodes[0],last=nodes.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus()}}return false}
