// A true six-face cube. Each authoritative result rotates its face to the top; camera tilt is separate.
export const DICE_ROT469={1:[90,0],2:[90,-90],3:[0,0],4:[180,0],5:[90,90],6:[90,180]};
const pips={1:[5],2:[1,9],3:[1,5,9],4:[1,3,7,9],5:[1,3,5,7,9],6:[1,3,4,6,7,9]};
export function die469(value=1,rolling=false,index=0){const [x,y]=DICE_ROT469[value]??DICE_ROT469[1];return `<span class="sg-die-scene469 ${rolling?'is-rolling':'is-waiting'}" style="--rx:${x}deg;--ry:${y}deg;--die-i:${index}" role="img" aria-label="サイコロ 上の面 ${value}"><span class="sg-die-hop469"><span class="sg-die-cube469">${[1,2,3,4,5,6].map(n=>`<span class="sg-die-face469 face-${n} ${n===value?'is-result470':''}" data-face="${n}">${Array.from({length:9},(_,i)=>`<i class="${pips[n].includes(i+1)?'is-pip':''}"></i>`).join('')}</span>`).join('')}</span></span></span>`;}
export function deck469(){return'<span class="sg-deck469" aria-hidden="true"><i></i><i></i><i></i></span>';}
export function fitPlayers469(players,nodes,width,height){const points=players.map(p=>nodes[p.pos]).filter(Boolean);if(!points.length)return{zoom:.72,x:0,y:0};const xs=points.map(n=>n.x),ys=points.map(n=>n.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);const zoom=Math.max(.03,Math.min(.85,(width-116)/Math.max(1,maxX-minX),(height-145)/Math.max(1,maxY-minY)));return{zoom,x:width/2-(minX+maxX)/2*zoom,y:height/2+26-(minY+maxY)/2*zoom};}
export function defenseResult469(e){if(e.reflected469)return'攻撃を相手へ跳ね返した！';if(e.effectType==='discard')return`破棄 ${Math.abs(e.n??1)}枚 → 0枚`;if(e.effectType==='steal')return`奪取 ${Math.abs(e.n??1)}枚 → 0枚`;if(e.effectType==='move')return`後退 ${Math.abs(e.n??0)}マス → 0`;return'この効果を無効化';}
