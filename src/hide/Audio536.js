export function unlock536(u){if(!u.sound)return;try{const Audio=globalThis.AudioContext??globalThis.webkitAudioContext;if(!Audio)return;u.audio??=new Audio();u.audio.resume()?.catch(()=>{})}catch{}}
export function sound536(u,type){if(!u.sound||u.audio?.state!=='running')return;const a=u.audio,notes={select:[330],begin:[220,330,440],catch:[523,659,784],miss:[145,100],noise:[660,520],smoke:[190,130],pulse:[300,600],seal541:[440,659,880],alarm559:[220,330,220],gates559:[392,523,659,784,1047],rescue541:[523,784],finish:[392,523,659,784]}[type];if(!notes)return;notes.forEach((n,i)=>{const o=a.createOscillator(),v=a.createGain(),t=a.currentTime+i*.075;o.type=type==='miss'?'triangle':'sine';o.frequency.setValueAtTime(n,t);v.gain.setValueAtTime(0,t);v.gain.linearRampToValueAtTime(.05,t+.012);v.gain.exponentialRampToValueAtTime(.001,t+.22);o.connect(v);v.connect(a.destination);o.start(t);o.stop(t+.24)})}
