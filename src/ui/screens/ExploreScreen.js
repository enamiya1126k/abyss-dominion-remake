import{APP_VERSION}from"../../core/config.js";
export function ExploreScreen(state){const p=state.party.map(id=>state.monsters.find(m=>m.id===id)).filter(Boolean);return`<section class="screen explore-screen"><header class="explore-hud"><div><small>階</small><b>${state.player.currentFloor}</b></div><div><small>G</small><b id="goldHud">${state.player.gold}</b></div><div><small>魔晶石</small><b>${state.player.crystals}</b></div><div><small>捕獲</small><b id="captureHud">${state.inventory.captureCrystals}</b></div><div class="version">v${APP_VERSION}</div></header><div class="explore-party-strip">${p.map(m=>{const need=40+m.level*25;return`<div><b>${m.nickname}</b><span>Lv.${m.level}</span><i><em style="width:${Math.min(100,m.exp/need*100)}%"></em></i><small>${m.exp}/${need}</small></div>`}).join("")}</div><div class="explore-stage"><canvas id="gameCanvas"></canvas><canvas id="miniMap"></canvas><button id="miniMapToggle" class="minimap-toggle">${state.settings.minimapVisible?"MAP ON":"MAP OFF"}</button><div id="encounterHint" class="encounter-hint"></div></div><nav class="explore-nav"><button id="pauseParty">編成</button><button id="fieldEquipment">装備</button><button id="pauseItems">持ち物</button><button id="centerCamera">現在地</button><button id="returnHome" class="danger">帰還</button></nav></section>`}