export function unlock542(u){if(!u.sound)return;try{const Audio=globalThis.AudioContext??globalThis.webkitAudioContext;if(!Audio)return;u.audio??=new Audio();u.audio.resume()?.catch(()=>{})}catch{}}
export function sound542(u,type){if(!u.sound||u.audio?.state!=='running')return;const a=u.audio,notes={place:[220],kick:[340,700],blast:[100,65],out:[160,95,60],go:[440,660,880],roundEnd:[523,659,784],shield:[740,988],guard:[620,440],collapse:[110,80]}[type];if(!notes)return;for(const[n,i]of notes.map((n,i)=>[n,i])){const o=a.createOscillator(),v=a.createGain(),t=a.currentTime+i*.065;o.type=type==='blast'||type==='out'?'triangle':'sine';o.frequency.setValueAtTime(n,t);o.frequency.exponentialRampToValueAtTime(Math.max(40,n*(type==='blast'?.35:1.15)),t+.18);v.gain.setValueAtTime(.0001,t);v.gain.linearRampToValueAtTime(.035,t+.008);v.gain.exponentialRampToValueAtTime(.0001,t+.22);o.connect(v);v.connect(a.destination);o.start(t);o.stop(t+.24);o.onended=()=>{o.disconnect();v.disconnect()}}}
