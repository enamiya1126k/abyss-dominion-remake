export function unlock517(u){if(u.sound===false)return;try{u.audio??=new(window.AudioContext||window.webkitAudioContext)();if(u.audio.state==='suspended')u.audio.resume().catch(()=>{})}catch{}}
export function cue517(u,type){const c=u.audio;if(!c||c.state!=='running'||!u.sound)return;try{const o=c.createOscillator(),g=c.createGain(),now=c.currentTime,low=['slam','crush'].includes(type);o.type=low?'triangle':'sine';o.frequency.setValueAtTime(low?110:type==='escape'?640:type==='bounce'?480:300,now);o.frequency.exponentialRampToValueAtTime(low?32:type==='escape'?1100:550,now+.15);g.gain.setValueAtTime(low?.10:.035,now);g.gain.exponentialRampToValueAtTime(.001,now+.22);o.connect(g);g.connect(c.destination);o.start();o.stop(now+.23)}catch{}}
export function close517(u){u.audio?.close().catch(()=>{});u.audio=null}
