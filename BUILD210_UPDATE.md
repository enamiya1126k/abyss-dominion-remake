# build210 最小差し替え

`v2.11.45 build210` は、戦闘中の情報表示と武器装備規則だけを更新する互換アップデートです。セーブキーとセーブスキーマ `58` は変更していません。

## 修正内容

- スキル演出を「使用者名／技名／実際の効果値」に分離し、長い技名は途中で不自然に改行せず縮小表示します。
- 浄化技を含め、説明文ではなく戦闘計算に使う値から効果表示を作ります。
- 階層ボス武器の固有能力を、戦闘開始ログ・開始演出・味方カード内の常設バッジ・条件成立時の金色表示へ接続しました。
- MP軽減は軽減前後の消費量をログへ表示し、装備限定スキルには `【装備技】` を付けます。
- 全武器を右手・左手どちらにも装備可能にしました。同一個体の二重装備は不可、別個体2本の通常能力値は両方加算、同一固有能力だけは重複しません。
- 階層環境の小表示へ、実データと同じ階層属性・有利属性・不利属性のマークと `+22% / -16%` を表示します。

## 実効果の確認

- 属性威力・全属性威力・ボスダメージ・スキル威力はダメージ式へ反映。
- MP消費軽減はスキル使用可否と実消費の両方へ反映。
- HP／ATK／DEF／SPDなどの割合補正、回復量、ガード、状態異常成功／耐性、吸収、会心、回避、命中などは既存の共通装備計算へ接続したままです。
- 同じ固有能力を持つ武器を左右へ装備しても固有能力は1回だけ、通常ステータスと通常スロット効果は2本分を計算します。

## 画像素材

新規画像は生成していません。属性マークは既存の属性アトラスを再利用しているため、画像生成元・最終プロンプトの追加記録はありません。

## 確認

- build210 専用回帰テスト
- 全ESM 97ファイル構文検査
- 戦闘画面ランタイムHTML生成（固有能力、有利／不利属性、補正値）

