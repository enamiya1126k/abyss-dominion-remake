ABYSS DOMINION Build413 / v3.1.93
② 魔法陣・キャラの表示修正

適用対象：Build412（v3.1.92）導入済みゲーム。
展開した内容を、ゲームのindex.htmlがあるフォルダーへ同じ階層で上書き。
差分ZIPのため、既存ゲームのフォルダー全体は削除しないでください。
セーブ初期化は不要。保存形式84を維持。公開作業は行っていません。

修正内容
・編成の「第二章装備・魔法陣の組み合わせ」説明欄は、文字を折り返し、
  開閉と内容に合わせて高さを確保。4列の狭いカードでも操作欄へのはみ出しを防止。
・画像1枚だけの第二章／レイド魔法陣を、3枚切替の点滅処理から分離。
  「より」に装着した第二章魔法陣を含め、1枚画像を常時表示。
・ホームと戦闘で魔法陣→キャラ→名前・表示の描画順序を明示。
・通常の味方キャラも、敵と同じ可視領域を測るサイズ調整に対応。
  透過余白を除いた絵の高さは画面幅に応じて54〜88pxを目標とし、
  名前とHPカードの間の高さ・横幅を上限として調整。
  魔法陣は調整後のキャラ中心と大きさへ追従。
・専用ボスの既存サイズ処理、キャラの向き、戦闘能力は維持。

検証
表示処理8件成功。320/390/430/768/1280px幅のサイズ計算、
足元とHP欄の位置関係、繰り返し配置時の安定性、1枚／3枚画像の区別、
更新用キャッシュを検証。DOMの境界はテスト用の矩形・代替要素を使用。
前回の操作・合成テスト23件成功。旧Build412固定のバージョン検証1件は除外し、
Build413用の更新設定テストで検証。変更JSの構文確認成功。
iPhone実機／ブラウザー描画による目視確認は未実施。
以前の累積パッチと関連パッチから必要ソースを復元したため、ゲーム全体の
起動・オンライン通し確認は今回の検証に含まれません。

再実行（ゲームフォルダーでNode.js 24）
node --test tests/build413-display.test.mjs
node --test --test-skip-pattern="boot cache" tests/build412-operations.test.mjs

今回の差し替えではBuild412のmain.jsを変更しません。
①の操作修正を先に適用してください。③以降は今回に含みません。
