# build216 最小差し替え更新

- 戦闘画面下部の常設戦闘履歴を非表示ではなく描画対象から削除し、空いた高さをフィールドへ追加。
- 階層ボス／レイドボスの名前が2行になる場合に備え、名前・スプライト・ステータス枠を同じ量だけ下へ移動。
- 420px以下では名前幅と文字サイズを追加調整し、ボスの頭部と名前の上端見切れを防止。
- 戦闘処理用のログ配列、行動入力欄、前版build215の空参照防止処理は維持。

Web版：`v2.11.51 build216`  
セーブキーとセーブスキーマ（58）は変更していません。
