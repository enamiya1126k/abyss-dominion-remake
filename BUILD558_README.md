# ABYSS DOMINION Build558 / v3.1.237

Build557への差分パッチ。絨毯の奥へすぼまる角度を、背景の道路に合わせて修正した。

## 適用

Build557をバックアップしてから、ZIPの内容をプロジェクトのルートへ上書きし、本体を再読み込みする。ZIPに含まれない既存ファイルも必要。サーバーの更新・再起動は不要。カートの通信形式は7のまま。

公開サイトへの配信は未実施。

## 修正

- 元の絨毯画像はそのまま使用。背景の床の両端から投影位置を合わせ、絨毯の長辺・縁取り・模様が道路と同じ方向へすぼまるようにした。
- 以前は横幅だけを奥ほど縮め、模様の縦間隔は一定だった。今回は四隅の幅から奥行き方向も補正し、奥の模様ほど詰まり、手前ほど広がるようにした。
- 氷の港を使うコースでは、床・カート・接地影・障害物・金枠・操作方向が同じ投影を使う。絨毯だけを傾けて、摩擦範囲や車の位置と食い違わせない。
- ラウンド、摩擦、衝突、発射の強さ、コース抽選、前回修正した崖際の配点は変更していない。サーバーとピンボールも変更なし。

床の基準は1024×1536の `assets/cart556/frost-harbor.png`。奥の床端を y=578、左右 x=373/626、手前の延長を y=1536、左右 x=83/943 として絵の直線部分に合わせた。背景の上20%を切る既存表示と同じ座標変換を使う。画面内の縦方向の移動量は従来通り。

## 確認

ブラウザーの実ゲームモジュールで絨毯の角度を確認し、マウスによる引っ張り操作1人＋AI3人で通常のランダム5ラウンドを完走した。時刻だけを早送りし、コース・得点・位置・結果は注入していない。検査記録は `docs/build558/`。

320×690、390×664、430×932、844×390で表示のはみ出しがないこと、通常コースとの切り替え、実走行中のカメラ拡大でも床と絨毯の角度がそろうことを確認した。操作で送信された発射角度・強さも検査用の指定値と一致し、ブラウザーエラーは0件。画像素材・ルール・操作処理・サーバーがBuild557と同じ内容であることも比較した。

ローカル検査は既存の実装を使用し、この作業環境にない別ゲームの依存だけを試験用に補っている。公開環境への適用とiPhone Safari実機の確認は未実施。

```sh
python tools/build558/static-check.py --baseline /path/to/Build557
```

ブラウザー検査は `PLAYWRIGHT_MODULE`、`PLAYWRIGHT_WS_MODULE`、`CHROMIUM_PATH`、必要に応じて `QA_FONT_PATH` を設定し、`tools/build558/visual.mjs` と `capture.mjs` を実行する。試験サーバーは127.0.0.1:8558。
