# ABYSS DOMINION Build557 / v3.1.236

Build556への差分パッチ。暴走ショッピングカートの絨毯を作り直し、金枠を越えて安全に止めるほど高得点になるよう配点を修正した。

## 適用

1. Build556をバックアップし、ZIPの内容をプロジェクトのルートへ上書きする。ZIPに含まれない既存ファイルも必要。
2. 本体と `online-server` の両方を更新し、オンラインサーバーを再起動する。
3. 参加者全員が本体を再読み込みして準備OKから開始する。

カートの通信形式は7。異なる配点を同じ対戦に混ぜないため、旧形式の進行中カート対戦は準備画面へ戻る。パーティーのメンバーと終了済みの得点記録は維持する。公開サイトへの配信・サーバー再起動は未実施。

## 配点

旧版は金枠内の約140〜146点に対し、金枠を越えた安全な停止位置が約97〜100点まで下がっていた。新版では金枠の＋50点を奥側でも維持し、崖に近づくほど０〜50点を追加する。

| 停車位置の例 | 通常ラウンドの合計点 | 最終ラウンドの合計点 |
| --- | ---: | ---: |
| 車体全体が金枠に入った位置 | 140 | 280 |
| 車体全体が金枠に収まる最も奥 | 146 | 292 |
| 金枠の奥から崖際までの中間 | 173 | 346 |
| 落下直前のごく狭い位置 | 最大200 | 最大400 |
| 落下 | 0 | 0 |

- 基本点０〜100点＋金枠ボーナス50点＋崖際追加０〜50点。整数へ丸めるため点数が同じになる小区間はあるが、安全な停止位置では奥へ進むほど点数が下がることはない。
- ボーナスは実際に停車した車に対して判定する。過去に金枠や崖際へ入った履歴を加算する方式ではない。
- 追突で押し戻されれば、その最終位置に応じて減点。落下した回は基本点もボーナスも０点。既に終えたラウンドの点数は残る。
- ５回目はボーナスを含む合計を２倍にする。全ラウンド合計点で順位を決定する。
- 画面の仮得点、停車時の演出、採点画面、最終結果は同じ計算を使う。金枠は★、その先の「キワ停め」は◆で示す。
- 20秒の制限時間に達した場合は、その位置で移動を止めて採点する。確定得点と停止後の表示が食い違わないよう統一した。

## 絨毯

単純な菱形と線で描いていた床を、織り目・花柄・縁取り・房のある画像へ差し替えた。新素材は `assets/cart557/woven-runner.png`。元の透過PNGを保持し、ゲームの道路と同じ遠近法で描画する。薄い接地影と環境光を加え、画像が読み込まれた時と画面サイズ・コースが変わる時だけ床を描き直す。

絨毯と氷の範囲、摩擦、12コースの内容、５ラウンドの抽選、引っ張り操作、発進前の衝突、勢いを乗せた発射、MAXの強さは維持した。着地点の予測ガイドは表示しない。

## 確認

- Nodeテスト40件成功。29万以上の停止位置の配点、金枠と落下境界、押し戻し・落下、重複加点防止、最終２倍、保存復旧、全12コース、既存の衝突、パーティーと通信経路を確認した。
- Chromiumの４接続で140・146・173・200点の確定と、最終回280・292・346・400点を確認した。得点欄、採点画面、結果履歴、順位は全クライアントで一致した。
- ５ラウンド完走、再接続、再戦、タッチとキーボード操作、動きを減らす設定を確認した。
- 320×690、390×664、430×932、844×390で操作欄が収まり、横はみ出しがないことを確認した。

実操作キャプチャは１人分の自動マウス操作＋３AIによる通常のランダム５ラウンド。時刻を早送りし、コース・位置・得点・結果は注入していない。これとは別に、境界値の採点検査では４台の位置を指定した試験を行っている。

ローカル検査は既存プロジェクトの実装モジュールを使用し、この作業環境にない別ゲームの依存だけをテスト用の外側で補っている。公開環境への適用、iPhone Safari実機の確認は未実施。

制作には組み込みの画像生成を使用した。プロンプト、元画像のハッシュ、確認記録は `docs/build557/` に同梱した。

```sh
node --test tests/build557-*.test.mjs tests/build556-courses.test.mjs tests/build555-main-wire.test.mjs
python tools/build557/static-check.py --baseline /path/to/Build556
```

ブラウザー検査には `PLAYWRIGHT_MODULE`、`PLAYWRIGHT_WS_MODULE`、`CHROMIUM_PATH`、必要に応じて `QA_FONT_PATH` を設定し、`tools/build557/browser-qa.mjs`、`visual.mjs`、`capture.mjs` をそれぞれ実行する。テストサーバーは127.0.0.1:8557で待ち受ける。
