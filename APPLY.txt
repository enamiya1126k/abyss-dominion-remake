ABYSS DOMINION v0.8.0-alpha.2.2 startup hotfix

Files to replace:
- index.html
- src/main.js

Files to add:
- src/app.bundle.js

The entry script now loads from the new bundled filename, so Safari cannot reuse the broken cached module.
