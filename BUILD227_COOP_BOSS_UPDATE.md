# BUILD 227 / RELEASE 168 — 共闘ボス

## 実装方針

- 既存の共同探索を置き換えず、2人以上で遊ぶときだけ任意の寄り道ボスを追加します。
- 1人オンラインでは関連オブジェクト・案内・戦闘・報酬を生成しません。
- 10階ごとの通常階層ボスとは同じ階に出現しません。
- 共闘ボス撃破は通常探索の階層進行や通常ボス初回撃破へ干渉しません。

## 共闘ボス

| ボス | 連携課題 | 成功効果 |
|---|---|---|
| 雷鎖牙獣・ライガルム | 同じ課題ラウンドに2人以上で攻撃 | ボスへ被ダメージ+35% |
| 凍鏡騎王・グラシエル | 同じ課題ラウンドに2人以上で防御 | 生存者全員へ最大HP10%の障壁 |
| 虚環門主・アルカノス | 攻撃役と支援役が同じ課題ラウンドに行動 | ボスへ被ダメージ+35% |

- 連携課題は3ラウンドごとに発生します。
- 失敗時はボスの攻撃力が25%上昇します。
- ボス固有の能力値、属性、行動セット、演出名を実装しています。
- 既存モンスターの8フレーム画像を利用し、探索と戦闘の両方へ表示します。

## UX

- 挑戦地点でボス名、異名、攻略条件を確認してから開始します。
- 戦闘画面へ課題名、残りラウンド、成功数、失敗数を常時表示します。
- 共闘ボス中は専用ボス演出を使い、通常のLINK BREAK表示との重複を避けます。
- サーバー判定だけでなく、クライアントの捕獲ボタンも無効化します。
- 390×667相当の小画面、セーフエリア、低モーション設定へ対応します。

## 敗北・切断・再接続

- 全滅しても共同探索そのものは終了しません。
- HP25%・MP20%で挑戦地点へ戻り、同じボスへ再挑戦できます。
- 明示的な退出で戦闘可能人数が2人未満になった場合は、安全に戦闘を中断します。
- 一時切断では既存のAI代理を利用し、猶予時間内の再接続で同じ戦闘へ復帰します。
- 切断中に勝利した参加者の報酬は保留され、再接続後に受け取れます。

## 報酬と安全性

- 固定追加報酬は2人3個、3人4個、4人5個の魔晶石です。
- 100階以上では捕獲結晶1個を追加します。
- 装備、深淵の鍵、階層解放、通常ボス初回報酬は発生しません。
- 報酬IDは戦闘・ボス・参加者単位で固定し、再送信や勝利処理の再実行による二重取得を防止します。
- 撃破後に出現する豪華共鳴宝箱は、ボス固定報酬と分離しています。

## 互換性

- セーブスキーマは変更していません。
- オンライン通信プロトコルは `1.14.0` です。
- ゲームとオンラインサーバーは同じRELEASE 168のものを組み合わせてください。

## 検証

- サーバー全テスト71件合格（実WebSocket 4接続を含む）。
- 現行クライアント回帰24件合格。
- JavaScript ESM構文検査97ファイル合格。
- 3ボスの決定的ローテーション、全連携条件、捕獲不可、再挑戦、人数別報酬、二重受取防止、切断復帰を検証済みです。
