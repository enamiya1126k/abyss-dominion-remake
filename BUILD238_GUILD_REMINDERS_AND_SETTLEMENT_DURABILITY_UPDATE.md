# build238 ギルド遠征通知・受取保全

## ギルド遠征の当日通知

- 「参加予定」または「未定」を選んだ遠征は、開始30分前から交流ボタン、ギルドタブ、予定一覧で目立つように表示します。
- ギルド内で参加可能な集合が始まると「集合中」として案内し、予定カードから従来どおり手動で部屋へ参加できます。
- 通知時刻はサーバー時刻を基準にし、端末時計がずれていても開催可能時間と画面表示が食い違いにくいようにしました。異常な時刻値は採用せず、端末時刻へ安全に戻します。
- 同じ予定・同じ段階の案内は端末ごとに1回だけ表示し、再接続や画面更新でトーストを繰り返しません。履歴は上限付きで端末内に保持します。
- OS通知、通知音、自動入室、自動出発は行いません。参加操作の直前にも所属、空き枠、部屋状態をサーバーで確認します。
- 主催者は集合中の予定カードから「集合を終了」できます。永続している遠征予定や参加表明は取り消しません。

## 交換・未受取報酬のサーバー再起動保全

- 交換の受渡し開始前に、取引ID、双方の提示内容、端末ごとの受取確認状態をサーバーへ原子的に保存します。
- 片方だけが受取保存を完了した直後にサーバーが再起動しても、未確認側だけへ同じ取引IDで再送します。確認済み側へ二重配布しません。
- オンライン報酬と確定済み通知も、端末が受取確認するまで上限付きの復旧待ちとして保存します。
- 復旧データは既定で `online-server/data/settlements.json` に保存します。保存先は `SETTLEMENT_STATE_FILE` 環境変数で変更できます。
- 端末キーと再接続キーは平文で保存せず、照合用ハッシュだけを記録します。壊れた保存ファイルは隔離し、`/health` を異常状態にして管理者へ知らせます。
- 保存件数、1件の大きさ、全体容量、保持期間を制限します。未受取報酬・確定済み通知は24時間、受渡し開始後の未確認交換は7日間を上限に復旧できます。
- 保存に失敗した場合は、新しい受渡しや報酬通知を送る前に処理を止めます。

## 互換性

- Web版は `v2.11.64 build238` です。
- オンライン通信は既存の `protocol 1.16.0` と互換です。
- `guilds.json` はversion 3、通常セーブはschema 58のままです。
- 通常探索、報酬内容、交換できる品目、ギルド報酬、週間共闘Ptの計算は変更していません。
- 差分ZIPに `online-server/data/*.json` は含めません。更新時は既存の保存データを削除せず、オンラインサーバーを再起動してください。
