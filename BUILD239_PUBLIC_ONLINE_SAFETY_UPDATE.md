# build239 公開オンライン安全機能

## 更新の目的

公開募集で初めて会うプレイヤーとも安心して遊べるように、build232で追加したブロック基盤をオンライン全体へ接続し、ミュート、一覧管理、解除、再参加防止までを一つの安全機能として完成させました。

## ミュートとブロックの違い

| 操作 | 相手の表示 | フレンド・招待・交換 | 同じ部屋への参加 | 相手への通知 |
| --- | --- | --- | --- | --- |
| ミュート | 相手のチャット、吹き出し、スタンプ、探索ピンを自分の画面で非表示 | 変更なし | 参加可能 | 通知しない |
| ブロック | ミュート対象として扱い、相手の交流表示を非表示 | フレンド関係、申請、招待、新しい交換を停止 | 公開募集、おまかせ参加、ルームID、招待、ギルド募集の各経路で再参加を防止 | 誰がブロックしたか通知しない |

ミュートは表示だけを静かにする機能です。相手のキャラクター、HP、戦闘行動、救助、宝箱、進行に必要なシステム通知は隠しません。ブロックはオンライン上の接触そのものを止める機能で、解除しても以前のフレンド関係、申請、招待は自動では戻りません。

## プレイヤーから使える操作

- 同じ部屋の参加者、フレンド、届いた申請、ギルドメンバーからミュートまたはブロックできます。
- 交流パネルの「安全設定」で、ミュート中とブロック中の相手を確認し、それぞれ解除できます。
- ミュート中の相手が送った部屋チャット、探索チャット、吹き出し、スタンプ、探索ピン、ギルドチャットは、未読件数にも加えません。
- 名前やメッセージは従来どおり無害化して表示し、安全設定のボタンはモバイルでも押しやすい操作領域を維持します。

## サーバー側の再参加防止

ブロックは画面上の非表示だけではありません。サーバーは、どちらか一方が相手をブロックしている組み合わせを入室時に再確認します。

- ブロック関係のある参加者がいる部屋を公開掲示板へ表示しません。
- 「おまかせ参加」は、その部屋を候補から除外します。
- 古い掲示情報、ルームIDの直接入力、部屋招待、ギルド募集から参加しようとしても入室を許可しません。
- 新しいフレンド申請、部屋招待、交換も開始できません。
- 相手側へブロックした人を知らせないよう、参加できない場合は共通の案内を返します。
- ブロック一覧に、オンライン状態、現在の部屋、最終接続時刻などの追跡情報を含めません。

## 進行中コンテンツと交換の扱い

安全操作によって、進行中の通常探索、共闘探索、レイド、チーム戦、共鳴迷宮を強制終了しません。現在の進行と報酬を壊さないため、通信表示を止めたうえで、帰還または終了後から今後の同室参加を防ぎます。

受渡し開始前の未確定交換は安全に取り消し、退避中の品を返却します。双方が最終確認し、受渡しを開始した確定交換は巻き戻しません。release179で追加した `settlements.json` の受取保全を優先し、未受取側だけへ同じ取引IDで復旧します。

## 保存と旧データの互換性

- フレンド、申請、ブロック、ミュートは、既定では `online-server/data/friends.json` に保存します。
- 保存形式はversion 2です。
- version 1の既存`friends.json`は起動時に自動で読み込み、不足しているミュート一覧を空で補います。既存アカウント、フレンド関係、申請、ブロックは維持され、次回保存時にversion 2へ更新されます。
- 保存件数と操作頻度には上限があり、保存失敗時はメモリ上だけ成功した状態にしません。
- 差分ZIPには実際の`online-server/data/*.json`を含めません。更新時に`data`フォルダを削除したり、空のデータで上書きしたりしないでください。

## 今回の非対象

- 運営窓口へ送る通報機能
- 管理者によるアカウントBAN、IP BAN、端末BAN
- 自動NGワード判定や長期の監視ログ
- ブロックを理由にしたギルドからの自動除名
- 進行中コンテンツの強制終了

このオンラインサーバーは自宅運用にも対応しており、共通の審査担当者や通報受付先を持ちません。そのため、届かない通報ボタンや形だけの制裁機能は追加していません。

## 互換性

- Web版は `v2.11.65 build239` です。
- オンライン通信は既存の `protocol 1.16.0` を維持します。
- 通常セーブはschema 58のままです。
- `friends.json` はversion 1からversion 2へ自動移行します。
- `guilds.json` はversion 3、`settlements.json` はversion 1のままです。
- 通常探索、報酬内容、交換品目、所持品、GOLD、魔晶石、週間共闘Ptの計算は変更していません。

## 差分パッチの適用

1. 進行中のオンラインプレイと交換を終え、オンラインサーバーを停止します。
2. `online-server/data`フォルダ全体を別の場所へバックアップします。
3. release180の差分ZIPを展開します。
4. ZIP内の`abyss-dominion-remake-main`フォルダを開き、その「中身」をrelease179適用済みの同名フォルダへ、階層を保ったまま上書きします。
5. `online-server/data`フォルダと既存のJSON保存ファイルを削除しないことを確認します。
6. オンラインサーバーを再起動します。
7. ブラウザを再読み込みします。
