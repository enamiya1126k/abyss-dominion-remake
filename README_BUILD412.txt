ABYSS DOMINION Build412 / v3.1.92
対象：Build411（v3.1.91）導入済みのゲーム

適用：ZIPを展開し、index.htmlのあるフォルダーに同じ階層で上書きしてください。
これは差分ZIPです。ゲーム全体を削除・置き換えないでください。
セーブ初期化不要、セーブ形式84を維持。公開作業は含みません。

① 操作・合成の不具合
・合成から開く保護管理で、お気に入りのみ残った装備も表示。
・「ロック・お気に入りを解除」で選択した装備の両方の保護を解除。
  合成画面→ロックをまとめて管理→素材にしたい装備を選択→解除→戻る。
  装備中／素材使用不可は引き続き素材にできません。解除だけでは装備は消費されません。
・スキル／アイテムの一覧を開く処理を行動処理の前へ分離。
  開くだけで万象抽選陣・勇者行動回数・ターン処理を進めません。
  アイテムは対象・使用可否の確認後に行動を開始し、二重タップの消費を防止。
・召喚結果の「閉じる」と右上×は導入／当たり表示／切替途中でも有効。
  取得済み報酬は全件維持し、閉じると残りの演出タイマーを解除。
・更新時の読み込み先をBuild412へ変更。

検証：24件成功。実装関数を抽出実行し、保護解除と保存失敗時の復元、
素材消費、メニューの無消費、実アイテム処理、ガチャ終了とタイマー、
キャッシュ設定を検証。DOM・保存・戦闘の外部境界はテスト代替を使用。
構文確認成功。iPhone実機／ブラウザー描画／ゲーム全体通し動作は未検証。
Build411の累積パッチから復元したソースを使用し、未同梱の基礎ファイルは変更していません。
以前のBuild411向けハーネスはソース固定のため、今回の検証は
node --test tests/build412-operations.test.mjs を使用してください。
②以降の表示・ペア・バランス修正は今回に含みません。
