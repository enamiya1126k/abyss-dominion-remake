# ABYSS DOMINION Build556 / v3.1.235

Build555への差分パッチです。「暴走ショッピングカート」を５ラウンドのまま、12コースから重複なしで抽選する形式に変更しました。７つの新コース、新しい背景３枚、ばね樽の画像素材を追加しています。

## 適用

1. Build555をバックアップし、このZIPをプロジェクトのルートに上書きしてください。ZIPにない既存ファイルも必要です。
2. 本体と `online-server` の両方を更新し、オンラインサーバーを再起動してください。カートの通信形式は6です。
3. 参加者全員が本体を再読み込みし、準備OKから始めてください。

古い形式の進行中カート対戦は準備画面へ戻ります。パーティーのメンバーは維持します。公開サイトへの配信・サーバー再起動はこのZIPには含みません。

## 遊び方

- 開始時に12種類から５つを抽選。同じ試合の中で同じコースは出ません。海辺・氷の港・蒸気工場・夜の市場のうち、少なくとも３種類の景色が登場します。
- 抽選結果はサーバーに保存。参加者全員が同じステージで遊び、再接続・保存からの復旧でも引き直しません。次のコースはラウンド開始時に分かります。
- 最後の得点２倍はラウンド５のルールです。最後のコースも固定せず、どのコースでも２倍になります。
- 引いた距離で強さ、逆方向に発進。止まる位置の予測表示はありません。待機中の車への衝突、動かされながら勢いを乗せて発射する操作、強いMAX発射も継続しています。
- ベルトは１枚ごとに１回作動。二段加速はそれぞれ踏めます。逆向きは減速方向へ、横向きは矢印の方向へ力が加わります。
- ばね樽は実際に弾き返す障害物です。ばねが縮む動き、衝突音、金色の波紋とともに壁や相手との連続衝突を狙えます。
- AIもコースとスタート位置に応じた角度・力加減で発射します。発射時刻や照準にはばらつきがあり、衝突で結果が変わります。

## 12コース

| コース | 仕掛け |
| --- | --- |
| はじめての坂 | 素直な木の坂。強さと発射のタイミングで駆け引き |
| 床、磨きたて。 | 坂の長い区間が滑るワックス床 |
| 搬入中につき、遠回り。 | 中央の大きな積み荷を壁反射で回り込む |
| 返品エクスプレス | 中央のローラーで１段加速 |
| 閉店ダッシュ | 滑る床の先に加速台。旧「閉店セール」を通常の抽選対象に変更 |
| つるつる氷の港 | 全面が滑る氷。弱い一押しも遠くへ |
| 氷か、じゅうたんか。 | 左半分は滑る氷、右半分は強く減速するじゅうたん |
| 雪どけジグザグ便 | 左右の積み荷を、氷と壁の反射で抜ける |
| 二段ロケット配送 | 手前左・奥右に別々の加速台。通る数を選ぶ |
| 行きすぎ返品ベルト | 手前で加速、奥の逆回転ローラーで減速 |
| 真夜中のばね樽 | ３つの金色のばね樽と壁で連続バウンド |
| 横取りクロスベルト | 右向き・左向きのベルトが横方向に弾く |

ロビーの「12コースを見る」で仕掛けを確認できます。試合結果には実際に走った５コースを記録します。

## 確認記録

- Nodeテスト46件と本体通信経路のテスト２件が成功。抽選1,000シード、全12コース×４スタート位置、床の摩擦、複数ベルト、逆向き・横向き加速、高速でのばね衝突、保存復旧、既存の発射・衝突・ピンボールを確認しました。
- Chromiumで４人接続し、準備・タッチ発射・コース同期・再接続・５ラウンド完走・最終２倍・結果一致・再戦を確認しました。
- 320×690、390×664、430×932、844×390で横はみ出しがなく、操作欄が収まることを確認。キーボードと動きを減らす設定にも対応しています。
- 実操作キャプチャは１人分の自動マウス操作＋３AIによる通常の５ラウンド。サーバーの時刻を早送りし、コース・位置・得点・結果は注入していません。別の全コース表示検査では、各コースを個別に指定しています。

この作業環境には既存プロジェクトの一部のみがあります。ローカル確認は実装モジュールを使用し、欠けている別ゲームの依存を接続用のテスト環境で補っています。公開環境への適用、iPhone Safari実機の確認は未実施です。

## 素材と再確認

新しい画像は `assets/cart556/` の `frost-harbor.png`、`foundry-quay.png`、`moonlit-bazaar.png`、`spring-barrel.png` です。背景は既存の道の構図を維持して制作し、仕掛けの当たり判定と描画は同じ座標を使います。制作プロンプトと画像ハッシュ、テスト記録は `docs/build556/` に含めています。

```sh
node --test tests/build556-*.test.mjs tests/build555-impact.test.mjs tests/build555-client.test.mjs tests/build555-main-wire.test.mjs
python tools/build556/static-check.py --baseline /path/to/Build555
```

ブラウザー検査には `PLAYWRIGHT_MODULE`、`PLAYWRIGHT_WS_MODULE`、`CHROMIUM_PATH`、必要に応じて `QA_FONT_PATH` を設定します。`node tools/build556/browser-qa.mjs`、`visual.mjs`、`capture.mjs` をそれぞれ実行できます。テスト用サーバーは127.0.0.1:8556で待ち受けます。
