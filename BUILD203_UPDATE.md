# build203 最小差し替え

build202へ、このZIPの中身をフォルダ構成ごと上書きしてください。

## 変更内容

- 戦闘中の向きを「味方＝右向き／敵＝左向き」に統一
  - `monsters`内の原画像はすべて右向きを前提にしています。
  - 反転は敵側の戦闘表示だけに適用します。
- 戦闘キャラの縦位置を微調整
  - 味方を約10px上へ移動
  - 通常敵は従来位置を維持
  - 階層ボスを約10px下へ移動
- 探索中の戦闘不能による、なつき度低下を変更
  - 同じ探索で1回目 `-100`
  - 2回目 `-150`
  - 3回目以降 `-200`
  - 全滅時の追加減少はありません。
- 自動戦闘でHPが減った際、回復スキル／回復アイテム／ガードを優先
- オンライン実プレイ画面を既存のソロ画面へ統合
  - 共闘探索：通常の探索画面・操作をそのまま使用し、他プレイヤーを同じマップへ表示
  - レイド：通常の戦闘画面・5つの操作ボタンをそのまま使用
  - 対人戦：自分側を左、相手側を右に固定し、通常の戦闘画面を使用
  - 部屋、待機、チャット、通信同期だけをオンライン専用処理として残しています。

## 注意

- このZIPにはmonster画像を含めていません。手作業で右向きへ統一した現在の画像をそのまま残してください。
- `online-server/src/RoomStore.js`も上書き対象です。オンラインサーバーを使用中の場合は、上書き後にサーバーを再起動してください。
- 反映されない場合はブラウザを再読み込みしてください。build番号は `2.11.38 / build203` です。

## 検証

- JavaScript構文検査：97ファイル合格
- build203向き・配置・なつき度・共通UI回帰検査：合格
- オンライン画面生成検査：2件合格
- オンラインサーバー検査：18件合格
- build202音声／戦闘レイアウト回帰検査：合格
