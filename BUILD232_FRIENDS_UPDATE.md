# build232 フレンド機能

## 実装内容

- フレンドIDによる申請、承認、拒否、解除、ブロック
- 相互フレンド一覧とリアルタイムのオンライン状態
- ロビーにいるフレンドから、現在の部屋への期限付き招待
- 招待受諾時の満員・進行中・フレンド関係の再検証
- 申請、フレンド関係、ブロック、端末照合情報のサーバー永続化
- 再接続・サーバー再起動後の状態復元
- 申請と招待のレート制限、上限、期限切れ・二重使用防止
- iPhoneのセーフエリアと44px以上の操作領域に対応したフレンドパネル

## サーバー保存

既定では `online-server/data/friends.json` に保存します。保存先は環境変数 `FRIEND_STATE_FILE` で変更できます。実データファイルは配布ZIPへ含めていません。

## 検証

- サーバーテスト: 133件合格
- 現行クライアント回帰: 48件合格
- 実WebSocket: 接続 → 申請 → 承認 → 部屋作成 → 招待 → 参加
- 永続化: 再起動後の関係復元、別端末キーによるID利用拒否
