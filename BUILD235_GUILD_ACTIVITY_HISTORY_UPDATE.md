# build235 ギルド週間目標・活動履歴

## 実装内容

- ギルドで今週どの共闘を遊んだかが分かる5種類の週間目標
- 成立した共闘とメンバーの出席を確認できる最新活動
- サーバー保存は最新80件、画面への送信は最新40件
- 最新活動は8件を初期表示し、必要な場合だけ残りを展開
- 出席を同じ日ごとにまとめる日次表示
- 旧`guilds.json`を壊さない起動時移行

ギルドの活動を報酬の取得画面にせず、「今週誰と何を遊んだか」を見返せる共闘記録として整理しました。

## 週間目標

| 対象のオンライン活動 | 目標 |
|---|---:|
| オンライン探索の踏破 | 3回 |
| 階層ボス／共闘ボスの撃破 | 3体 |
| ワールドレイドの勝利 | 1回 |
| 自由チーム戦の終了 | 3回 |
| 共鳴迷宮の踏破 | 1回 |

週間目標は毎週月曜09:00（JST）に新しい週へ切り替えます。判定にブラウザが自己申告した値は使わず、オンラインサーバーが成立を確定した活動だけを集計します。再送、再接続、画面の再読み込みによる二重計上を防ぎます。従来の週間共闘Ptとギルドレベルは互換性のため維持します。

目標の進捗はギルド内で共有するプレイ記録です。達成時にアイテム、GOLD、魔晶石、捕獲結晶その他の報酬は付与しません。

## 最新活動

活動履歴には、サーバーが成立を確定した対象活動の種類、日時、参加メンバーを記録します。記録は各ギルドの最新80件を`guilds.json`に保存し、クライアント用状態には最新40件だけを含めます。

画面では最新の8件を最初に表示し、展開操作で受信済みの残りを確認できます。メンバーの日次出席は、同一日の出席を1つの日付グループにまとめ、履歴を不必要に長くしません。

## 公開範囲とプライバシー

最新活動と週間進捗は、所属ギルド全員が見られる公開情報です。どの活動に誰が参加したかもギルド内で表示します。本機能は個人向けの非公開ログではありません。

所属していないプレイヤー、別ギルドのプレイヤー、ギルドID検索中のプレイヤーへは表示しません。ギルド外へルームIDや募集IDなどの非公開識別子を含めて送信しません。

## 保存と旧データ移行

ギルド本体、チャット、週間進捗、最新活動は`online-server/data/guilds.json`に保存します。build234以前の`guilds.json`に新しい項目が無くても、起動時に必要な初期値を補って読み込みます。旧ギルド、役職、メンバー、申請、招待、チャット、週間情報を維持します。

過去のサーバーログやブラウザ内履歴から活動を推測して遡及登録しません。最新活動の記録はbuild235適用後にサーバーが成立を確定した活動から始まります。

## ゲームデータへの影響

本機能はオンラインギルド内の進捗と履歴だけを更新します。アイテム、GOLD、魔晶石、捕獲結晶、装備、仲間は増減しません。通常探索の進行、所持品、プレイヤーの通常セーブに変更を加えません。

Web版は`v2.11.61 build235`、オンライン通信は互換性を維持する`protocol 1.16.0`、セーブスキーマは`58`です。

## 差し替え方法

release176は、release175適用済みのゲームフォルダへ上書きする差分パッチです。ZIPにはrelease175から変更・追加されたファイルだけを収録し、ゲーム全体、画像・音声などのゲーム素材、`online-server/node_modules`、実際の`online-server/data/*.json`は含めません。

ZIP内の`abyss-dominion-remake-main`フォルダの「中身」を、release175の同名フォルダへ階層を保ったまま上書きします。その後、オンラインサーバーを再起動し、ブラウザを再読み込みしてください。`online-server/data`フォルダは削除せず、旧`guilds.json`をそのまま残します。

## 検証対象

- 5種類の週間目標とそれぞれの上限
- サーバー成立イベントだけによる加算と二重加算の防止
- 月曜09:00（JST）の週間切替え
- 最新80件保存、最新40件送信、8件初期表示と展開
- 出席の日次グループ表示
- ギルドメンバー内公開とギルド外への非公開情報遮断
- 旧`guilds.json`の自動移行と従来データの維持
- プレイヤが画面から進捗や活動を改ざんできないこと
- 報酬、通貨、所持品、通常セーブが変化しないこと
