# ABYSS DOMINION Build555 / v3.1.234

Build554への差分パッチです。ポンコツ改造GPを終了し、人間ピンボールを「時間内に、狙って再発射する」ゲームへ変更しました。

## 適用

1. 既存のBuild554をバックアップし、このZIPをプロジェクトのルートへ上書きしてください。ZIPにない既存ファイルは必要です。
2. 本体と `online-server` を両方更新し、オンラインサーバーを再起動してください。ピンボールの通信形式は6になりました。
3. 全員が本体を再読み込みして、準備OKから開始してください。

既存のGP対戦は終了してミニゲーム一覧に戻ります。パーティーメンバーは維持します。旧ピンボールの進行中データは新しい準備画面へ戻します。新旧の物理を同じ試合で混ぜないための移行です。他ゲームのセーブを削除する処理はありません。公開サイトへの配信はこのZIPには含みません。

## 遊び方と変更

- **ラウンド・SHOT制を廃止。** 90秒の１試合。部屋主は150秒も選べます。途中で位置を戻したり結果画面を挟んだりしません。
- **2.8秒ごとに再発射。** 引く距離で強さ、引く方向の逆に発射。今ある速度へ発射の力を足します。待機中・能力を選んでいる相手にも当たり判定があります。接触でゲージを貯めると、強い再発射と4.2秒の自分の得点２倍。再び貯めれば同じ試合中に何度でも使えます。
- **３色 → 王冠。** 赤・青・緑を集めて上の王冠に当たると大当たり。自分に不足する色が光り、目標欄にも表示します。王冠は×1（60%）・×3（32%）・×7（8%）。残り15秒は王冠をさらに２倍。王冠の再使用は全員共通で4.5秒後です。
- **左右に外周レーン。** 下の入口から上へ３つの印を順番に通ると３色を獲得、完走は基本800点。通常のピンを狙うルートと使い分けられます。
- **衝撃で回る中央の棒。** 自動回転はありません。接触した位置・相対速度・回転慣性で、キャラクターと棒へ反対向きの力が働きます。軸付近ではほぼ回らず、端へ当てると強く回転。回っている棒は他のプレイヤーを弾き、その衝撃で減速や逆回転もします。
- **回転でFEVERを起動。** 実際に回転した角度でゲージを貯め、満タンで７秒間全員の得点２倍。直前に棒へ有効な回転力を与えた人に1,000点。味方も相手も動く共有装置です。
- **能力６種類。** 最初と30秒ごとに３枚から１枚。選択は７秒で自動決定。途中の追加能力はボタンで開き、選んでも移動・装填をリセットしません。各能力は最大Lv.3です。
- **盤面を拡幅。** 厚い額縁を新しい薄い金属枠・床画像へ変更。390×664の確認画面では盤面幅約364px（93.2%）。自分の駒は明るい輪と「あなた」で表示。大当たりは目標欄と得点ポップで知らせ、盤面を覆うパネルを出しません。結果は試合終了時に一度だけ出し、得点の内訳を確認できます。
- **GPは終了。** メニュー、新規作成、再戦の対象から外しました。旧対戦の片付けに必要な互換コード・未使用素材は既存環境に残る場合があります。

## 確認

- Nodeテスト44件成功。棒の衝撃・エネルギー・回転停止、高速接触、待機相手との衝突、慣性を乗せた再発射、装填・古い操作の拒否、外周の順番判定、得点の重複防止、90秒／150秒の連続進行、保存復旧、パーティー、既存カートの回帰確認。
- Chromiumの４ブラウザー接続で、準備・能力の非公開化・タッチ再発射・引いている最中の追加能力・棒の同期・再接続・大当たり公開・最終結果・再戦・GPの非表示を確認。
- 320×690、390×664、430×932、844×390でレイアウト確認。横はみ出しなし。キーボード、タッチキャンセル、フォーカス喪失、動きを減らす設定を確認。
- `docs/build555/` にテスト、ブラウザー、実操作キャプチャ、静的検査の記録があります。実操作キャプチャは１人分の自動操作＋３AI、時刻を早送りしています。位置や点数、能力、大当たりを注入して作った画面ではありません。

この作業環境は既存プロジェクトの一部です。ローカル確認には実装モジュールと接続試験用の外側を使用し、欠けている別ゲームの依存だけをスタブにしています。公開環境への適用、iPhone Safari実機の動作確認は未実施です。

## 再確認コマンド

```sh
node --test tests/build555-*.test.mjs tests/build547-cart-sling.test.mjs tests/build549-cart-physics.test.mjs
python tools/build555/static-check.py --baseline /path/to/Build554
```

ブラウザーの再現には `PLAYWRIGHT_MODULE`、`PLAYWRIGHT_WS_MODULE`、`CHROMIUM_PATH`、必要に応じて `QA_FONT_PATH` を設定し、`node tools/build555/browser-qa.mjs` を実行します。テスト用サーバーは127.0.0.1:8550だけで待ち受けます。
