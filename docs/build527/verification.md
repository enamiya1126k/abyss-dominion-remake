# Build527 検証記録

- `node --test tests/build527-sumo.test.mjs`：23成功、失敗0。
- AI100試合：全試合決着、全席勝利あり、状態・結晶数・イベント数の上限と有限値を確認した。
- Native Canvasで実際のRenderer523を実行し、幅320／375／390／430、通常・予告・落下・磁石・最終段階を描画した。
- View523とControls523はDOM／イベントのテスト用オブジェクト上で実行した。結果ボタン・ズーム防止・共通色・予測処理は本番コードを使った。外側のパーティーロビーだけはテスト用の空表示に置換した。製品コードには置換を適用していない。
- SumoCoordinator523はセッション・保存・通信のテスト用オブジェクトで検証した。実WebSocketの往復やユーザーのサーバーではない。
- `source-checks.txt`：構文、import map、CSS読込順、新キャッシュ、プロトコル、完成済みゲーム専用ファイルの同一性を確認した。
- `tools/build527/canvas-check.mjs` が描画記録を生成する。`field.png` 等はCanvas層のみの記録である。魔獣・名前・上部表示・中央ボタンはHTML層であり、この画像には含まれない。
- ブラウザ実行ファイルの取得が失敗したため、本環境ではブラウザの実レイアウトやiPhone Safari操作は未確認である。Canvas／DOM検証を実ブラウザ確認と同一視しない。

テストをローカルで再実行する場合は、ESMを読み込めるNodeと `@napi-rs/canvas` が必要となる。製品の実行時依存関係には追加していない。
