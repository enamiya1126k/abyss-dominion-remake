# Build523 検証記録

## 自動検証

実行コマンド（Node v24.19.0）：

```sh
node --test --test-skip-pattern='100 seeded' tests/build517-tower.test.mjs tests/build520-tower.test.mjs tests/build521-tower.test.mjs tests/build522-presentation.test.mjs tests/build523-sumo.test.mjs
```

95項目成功／失敗０。上記５ファイルで結果のskipは０。詳細はtests-regression.txt。新作単体は25項目（tests-sumo.txt）。追加で構文・import map・オフライン登録・既存ゲームのハッシュを確認（source-checks.txt）。

相撲AI100シード対戦：全100試合終了、勝利数は席順に19・23・25・33、引き分け０、平均70.73秒。対戦中の座標が有限であること、成長上限24・イベント40・結晶26以下を検証。実際の人間に対する難易度を保証する数値ではありません。

## 確認対象

- １〜４人参加、AI補充、所持キャラ選択、準備、部屋主開始。
- 対角移動の速度統一、溜め中の減速、溜め／回復の上限、キャンセル時の不発。
- １突進で同じ相手へ１回、正面衝突の相打ち、小さなキャラから最大成長キャラへの横撃。
- 即時場外、最後の１人／同時全滅、段階的崩壊、最大90秒。
- 旧通信・不正座標・重複入力の拒否、同時入力の保持、切断時AI、パーティーだけへの配信、再戦での旧対戦解放。
- 新作の実装をネイティブCanvas＋DOM/event fixtureに接続して、１本のRAF、画面非表示時停止、後処理、参照再利用、方向入力の予測と解除を検証。
- 二本指で移動と溜め、pointercancel／lostpointercapture／blur、無効なキー操作が次のタッチを妨げないこと。
- 選択／コピー／ドラッグ抑止は新作の操作部分に限定。相棒選択後はロスター・ホスト要素・祖先・ウィンドウのスクロールを復元。
- 勝利演出4.6秒から結果への遷移と、既存の再戦・一覧ボタン。
- 320×280、375×420、390×460、430×620の盤面キャンバス。背景と床のキャッシュ再利用、DPR上限1.5。
- Build522の完成済みゲーム専用コードは無変更。tests/build517-tower.test.mjsの一覧検証だけ、新作sumoを除いた既存７件の確認に更新。

## 実機確認との区別

実ブラウザー／iPhone／Safariは未確認。既存作業環境の不足３モジュールはインメモリfixtureで補った検証であり、製品ソースには追加していません。ネイティブCanvasの目視確認は闘技場・結晶・効果の描画についてで、完成版MonsterVisualによるキャラスプライトとブラウザー全画面の確認ではありません。

本体公開・online-server再起動・実公開環境の動作確認は未実施です。
