# Build512 verification

- Version: v3.1.191-build512. Incremental base: Build511.
- 274 automated tests passed; 0 failures, skips or cancellations. Full TAP report: tests.tap.
- 20 new scenery tests cover all seven thresholds, exact BigInt distances, viewer-specific scenery, the supplied screenshot distances, ordinary physical boundary crossings, enormous six-world jumps, reverse travel, final-boundary clipping, late join, reduced motion, immutable camera/markers/relative gaps, loader concurrency/caching/retry/cancellation/decode cleanup, two-slot parallax paint, zero repeated idle writes, actual View511 integration, finite RAF and hidden-tab cleanup, six complete image decodes and winner scenery.
- Existing 254 tests cover gameplay and the other minigames. 1,735 pre-existing files are byte-identical, excluding the four intentional integration/version/cache files. Rules511, Items511, Presentation510/511, the server and protocol, all original art and all other games are unchanged.
- Scenery is derived from the viewer's existing focusQ and server phaseAt. No distance cap, no camera scale change and no changed item arithmetic. Huge thresholds within a single frame get brief 280ms spacing between world arrivals; crossfade lasts at most 180ms. This only delays visual scenery and never anticipates forward distance. Late transitions are clipped at run end. The server run remains 4,800ms; no scene queue survives it.
- At most two scenery slots, each with three parallax depths. Both use courseOffsets511. One cached loader holds six new images, maximum two concurrent requests, 15-second request timeout, async decode and explicit listener cancellation on exit. Failed scenes have a color/lanes fallback. Core readiness does not depend on new images.
- Original generated PNGs are shipped without resizing or editing. Actual dimensions are recorded in generated-assets.json; all six are approximately 3:1. Total added raster bytes: 15,413,433 (about 14.7 MiB).
- JavaScript parsed with Acorn. Changed module aliases, main entry cache version, new stylesheet, offline asset entries and worker references checked. Archive CRC and extracted SHA-256 are checked by the packaging script.
- Real browser rendering is not verified for this build. The previous local-browser attempt in this workspace failed with net::ERR_BLOCKED_BY_CLIENT; no bypass or standalone-browser workaround was used. iPhone Safari layout, visual smoothness and device FPS remain unverified. DOM tests use instrumented lightweight nodes, not a rendering engine.
- The partial source checkout lacks original species/endgame/MonsterVisual dependencies. Tests use in-memory fixtures only; no replacement production dependencies are included.
- Frontend-only update. Existing Build511 server does not need restart. No deployment performed.
