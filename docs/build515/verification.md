# Build515 検証記録

Node.js v24.19.0、62 tests / 62 pass / 0 fail。TAP全文は `tests.tap`。

Build514の53項目（移動・衝突・期限・20問・出題・再接続・情報非公開・保存復元・DOM更新・入力解放・画像検査）を維持し、新しい演出9項目を追加した。

- 205/320/600px高のステージで、0秒直前までキャラ高＋10pxの隙間を維持。
- 0秒から140msで落下、着地後620msの砂埃と減衰する揺れ。閉鎖後は有限時間で停止。
- 3・2・1の秒境界と拡大、0でカウント強調を終了。
- 古い壁が下へ画面外へ去り、新しい壁と床が上から入る。床の最終位置と次の問題の開始配置が一致。
- 全員脱落・最後の1人・20問目では次の階を出さない。
- 画面の動きを減らす設定では急落・砂埃・揺れ・上下スクロールを抑制。
- クライアントが予測した0秒とサーバーの確定通知で衝撃音を二重発火しない。
- 新床と古い壁にフェードによる復元を使わず、床マークを中央へ配置するコードを確認。

JavaScript構文、import map、旧バージョンの別名、CSS読み込み順、オフラインキャッシュを確認。許可した変更対象以外の既存1,781ファイルはSHA-256一致。質問・正誤判定・歩行と衝突・サーバー処理を含むルールはBuild514と同一。

検証は画面モデル・DOMテストダブル・ソース検証。以前のローカルページへのブラウザーアクセスは `net::ERR_BLOCKED_BY_CLIENT` で拒否されており、実ブラウザーのスクリーンショット・iPhone Safari実機表示・FPS・4端末同時通信は未確認。

部分ソースに欠けている魔物データと描画モジュールはテスト時のメモリー内データのみを使用し、本番用の代用品は同梱していない。
