# Build536 検証記録

## 対象

保管済みBuild535 ZIPの全収録ファイルを、前作業の展開済み作業ツリーとSHA-256で照合し、一致を確認した。その作業ツリーを複製してBuild536を作成した。
新ミニゲームのルール、描画、操作、通信窓口と、既存パーティー／結果画面／クライアントへの接続を更新した。

## 新規ゲーム

`tests/build536-hide.test.mjs`: 14テスト成功、失敗0。

- 鬼1人・隠れる側3人、0〜3人のAI補充。
- 8秒の準備中の変身・移動と、鬼への情報秘匿。
- 鬼向けデータに隠れる人の座標・家具との対応・AI内部状態が含まれないこと。
- 範囲外・無効ID・連続送信・鬼以外からの攻撃拒否。
- 全員捕獲、12回の攻撃切れ、1人生存での時間切れと結果の二重確定防止。
- 小刻みな移動による気配リセットの抜け道防止。
- 煙幕中の家具秘匿、期限切れ、古いIDによる攻撃の拒否。
- 索敵の回数・クールダウン。
- 不正数値・古いゲームID・旧プロトコル・重複番号の拒否。
- 実際のパーティー管理処理による4人の参加・相棒選択・準備・開始・再戦・一覧への復帰。
- 切断後のAI代行・本人への操作復帰。
- 状態のJSON保存・再開後の同一結果、AI対戦30試合の完走。

AI同士の固定シード30試合では、鬼15勝・隠れる側15勝となった。これは局所的なバランス確認であり、人間同士の勝率を保証する数値ではない。

## ローカル実画面

Chromium 153.0.8010.0でモバイル・タッチをエミュレートした。
320×568、390×844、430×932の3画面サイズを確認した。

- 横方向のはみ出しなし。煙幕・変身・攻撃のボタンが画面内に収まる。
- 準備中の変身、床タップ移動、通常時の変身、煙幕の使用と残数反映。
- ダブルタップ後の画面倍率が1のままであること。
- 時間切れ、4人の結果表示、再戦、一覧へ戻る操作。
- 結果画面への遷移後、ズーム制限を解除すること。
- 別々の4画面を実WebSocketで接続し、鬼への座標秘匿、空振りで1回消費、3人捕獲、捕獲後の操作停止、鬼の勝利を確認した。
- 新ミニゲームに関するブラウザの実行エラー・素材読み込みエラーは0件である。

ローカル検証用サーバーは実際の`RaceCoordinator451`と`HideCoordinator536`を使用した。プレイヤー認証・本編起動はローカル用の窓口で代替した。既存の本編全体を起動した検証ではない。
旧本体の`species.js`等が作業ツリーに欠けていたため、確認用カタログ・小さな相棒アイコンを検証時だけ供給した。これらを本体の置換ファイルとして納品していない。
一覧へ戻る際の旧キャベツ画像`assets/cabbage484/kitchen.png`は元のBuild535作業ツリーにも存在せず、ローカル検証では404を記録した。今回の新ミニゲームの素材に欠落はない。
iPhone実機・Safari・公開環境での適用確認は未実施である。

## 既存機能の回帰確認

- Build462のパーティー／レース: 5/5成功。
- Build535の相撲: 16/16成功。
- Build534の釣り: 7/7成功。
- Build485の旧パーティー操作テスト: 12/15成功。3件はBuild528で追加済みのプロトコル指定を旧テストが送信しないため失敗する。同じ3件が変更前Build535でも失敗することを別途確認した。

旧相撲・釣りのゲーム本体・画像は変更していない。

## 納品

本体、サーバー、新素材、キャッシュ更新、テスト、検証記録、スクリーンショットを差分ZIPに収録する。
ZIP内のファイルは`BUILD536_MANIFEST.json`のSHA-256で検証できる。
`tools/build536/`のコードはローカル確認専用であり、本番サーバーから呼び出さない。
