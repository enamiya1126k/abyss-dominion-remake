# Build534 verification

実施日：2026-09-24。Build533の作業用ソースからの差分。

## 自動テスト

- `tests/build524-fishing.test.mjs`、525、526、530、531、532、533、534をまとめて実行：103 / 103成功（`tests.txt`）。
- `tools/build529/regression.mjs`：120 / 120成功（`regression-log.txt`）。レース、パーティー、AI、ブロック落とし等の既存の検証。旧ブロック落としテストの通信番号・チャージ時間は、現行仕様に合わせた一時ファイルで実行し、終了後に削除。
- 通常抽選60,000回：新32種類すべてに到達、変わり種とゴミの合計は４％付近。餌使用抽選10,000回で新しいゴミなし。
- ゴミ全８種類：餌・連鎖への切り替え不可、手動回収は１回分だけ得点、回収後も所持餌・竿経験値を維持。
- ゴミ釣り上げ後の選択待ち、５秒経過と試合終了時の自動回収を検証。
- 図鑑152項目、実在88、今回追加32、海辺の知恵24、海のゴミ８、危険・注意14。出典リンク96（実在88＋ゴミ８）。
- 通信７から８への移行で、釣っている魚・竿経験値・得点・予定ヌシを維持。

## ブラウザ

Chromium / Playwright、タッチ操作あり。320×568、390×700、430×800の３サイズ。

- 横方向のはみ出しがなく、操作領域が画面内に収まる。
- 図鑑の分類、全件スクロール、最終項目、新画像２枚の読込。
- 回収ボタンだけを表示するゴミの釣果画面、回収時の得点。
- 食中毒と毒のトゲの注意表示。
- 手動キャスト、食いつく前の回収、強化維持、ヌシ再挑戦。

スクリプト：`tools/build534/browser-qa.mjs`。結果：`browser-results.json`、`browser-log.txt`。390px幅の画面例PNGを同梱。

## バランス

`tools/build534/balance.mjs`で各100シード、２分の同条件を比較。

| 方針 | 平均得点 | 平均釣果数 |
| --- | ---: | ---: |
| 岸で小魚を確保 | 577.35 | 13 |
| 餌・竿を育て大物へ | 2653.58 | 8.64 |

大物へ進む価値が維持されていることを確認する試験。相手との競合や人間の入力遅れは含めていません。現実の勝率・体感難易度の推定には使えません。

## 作業用ソースの制約

既存の `src/data/species.js`、`src/data/endgameCharacters.js`、`src/ui/MonsterVisual.js`、`src/models/Monster.js` が作業用ソースにないため、プレビューと回帰テストではQA専用フィクスチャを使用。本番パスへ代替データを書き込んでいません。画面例のキャラは仮表示です。

実機Safari、公開サイトへの反映、複数端末の実通信は未確認。公開・サーバー再起動は行っていません。

