# Build513 検証記録

## 結果

- Node.js v24.19.0: 316 tests / 316 pass / 0 fail。
- 新ゲーム42項目、既存ゲーム274項目。TAP全文は `tests.tap`。
- 変更したJavaScriptの構文、import mapの全旧別名、エントリーのバージョン、CSS参照、オフラインマニフェストを確認。
- 許可した共通の接続箇所以外の既存1,742ファイルのSHA-256がBuild512と一致。
- 生成した3枚のPNGをデコードし、平台アトラス4セルの描画内容と透明領域を確認。

## 新ゲームの検証範囲

120問のID・文面の一意性、12ジャンル、〇×の内訳、難易度、文字数、出典URL。計算・文字列問題は別計算で検算。1,000回の問題抽選でジャンル重複と直近30問の再出題回避、全問題への到達を確認。

1〜4人＋AI、全員選択後も12秒を維持、選び直し、期限境界、順序が入れ替わった通信、重複通信、未選択脱落、全員同時脱落、最後の1人、10問同時優勝、過去の脱落順位を確認。500試合を疑似乱数で完走し、各遷移でJSON保存・復元後の状態一致を確認。

サーバーの実Coordinatorを使い、本人の席への入力、旧バージョン拒否、他の部屋への入力拒否、途中参加の観戦、接続切断、JSON再起動、保存失敗時の巻き戻し、再戦操作の冪等性、ゲーム切替と旧部屋の削除を検証。

現在の正解・未来の問題・AIの内部計画が判定前の公開データに含まれないことを確認。出題済みの問題だけが再戦の除外リストに入ります。

画面更新用のDOMテストダブルで、足場への移動先、途中の方向転換の連続性、壁の接近、締め切り時の操作停止、視差効果軽減、最大1本の有限RAF、非表示タブの停止、終了時のイベント解放、静止中1,000回の反復描画で不要なDOMプロパティ書き換えがないことを確認。連打をまとめ、最新の移動先だけを再送することを確認。

## 限界

この作業用の部分ソースには `src/data/species.js`、`src/data/endgameCharacters.js`、`src/ui/MonsterVisual.js` がありません。自動テストは、欠けている場合だけメモリー内のテスト用データを読み込んでいます。これらの本番ファイルを代替したり、偽の実装をZIPへ追加したりしていません。

Cloud Browserで `http://127.0.0.1:18498/` を開く操作は `net::ERR_BLOCKED_BY_CLIENT` で失敗しました。回避操作は行っていません。実画面のスクリーンショット確認、iPhone SafariのFPS計測、実際の4端末間通信は未実施です。レスポンシブCSSは実装済みですが、実機表示の確認済みを意味しません。

問題の出典は `question-sources.md`。生成画像のプロンプトと元ファイルのSHA-256は `generated-assets.json`。新ゲームのルール判定に第三者のクイズ配信サービスは使っていません。
