# Build524 検証記録

## 最新実行

Node v24.19.0。以下の121項目成功・失敗０。

```sh
node --test --test-skip-pattern='^100 seeded AI games' tests/build517-tower.test.mjs tests/build520-tower.test.mjs tests/build521-tower.test.mjs tests/build522-presentation.test.mjs tests/build523-sumo.test.mjs tests/build524-fishing.test.mjs
```

ログ：tests-final.txt。上記の除外は変更していないタワーの長時間100試合テストだけです。このテストも、直前の除外なし実行で成功しています（tests-regression.txt、112.6秒）。その後の修正は新作のエサ満タン表示、結果演出、観戦者操作ガード等に限定し、最新一式を実行しました。Nodeの出力では除外ケースは列挙されず、最新の実行件数が121です。

新作単体26項目成功（tests-fishing.txt）。釣りAIの100シード試合では全試合終了。勝利数は席順19・29・28・24、ぬしの確保合計565匹、１参加者あたり平均11.625匹を釣り上げ、平均0.2825回逃しました。実際の人間に対する難易度を保証する値ではありません。

## 確認した内容

- １〜４人とAI補充、全員の初期能力・色、魚影への投げ入れ・水面範囲・二重投げ入れ防止。
- ヒット待ち、巻く／緩める、暴れる予兆・暴れ・疲れ、糸切れ・逃走。
- 釣り上げと竿の成長、得点／エサの排他処理、二重報酬防止、自動確保、成長・エサ上限。
- 90秒のぬし出現、120秒以降の新規投げ入れ禁止、最大10秒の取り込み、釣れた魚の終了時確保、同点の同率順位。
- 座標や入力の検証、古い通信・重複入力・別の利用者や別ゲームからの操作拒否。
- 切断・ホームからAIへの引き継ぎと復帰、入力ごとの永続書き込みを避ける処理、パーティーに限定した配信、再戦と他ゲームへの切り替え。
- 実装コードをネイティブCanvas＋DOM/event fixtureへ接続し、１本のRAF、参照と画像のキャッシュ、画面非表示時停止、後処理を検証。
- 巻く／緩めるの同時操作、pointercancel・lostpointercapture・blur、操作部分に限定した選択・コピー・ドラッグ抑止。
- キャラ選択のスクロール保持、シーンを組み直さず釣果選択へ移る処理、5.2秒の釣果お披露目、観戦者の操作禁止。
- 320×240、375×380、390×430、430×600のキャンバス。描画倍率の上限1.5と、背景の再構築がサイズ／素材の読み込み変化時だけであること。
- 構文、import map、オフライン登録、既存ゲーム専用ディレクトリと既存CSSのバイト一致（source-checks.txt）。

## 見た目の確認と制限

生成素材を読み込んだネイティブCanvasで、池・魚影・糸・竿・ぬしの釣り上げを描画して目視しました。これはブラウザー全画面や実機の確認ではありません。既存MonsterVisual等３モジュールが不足しているため、全体起動の代わりにテスト専用インメモリfixtureを使っています。製品用の代用品は含めていません。

実ブラウザー／実iPhone／Safari／実回線４端末対戦は未確認。公開先の更新・サーバー再起動も未実施です。
