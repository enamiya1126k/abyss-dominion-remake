Build402 検証記録 / v3.1.82
基準：Build401 / v3.1.81。セーブスキーマ84を継続。

自動テスト
統合回帰380件（既存368＋今回12）、オンライン戦闘70件、セーブ保護・移行9件。
合計459件成功、失敗0。実行されたテストのスキップ0。
全テストファイルの一括実行ではなく、今回に関連する回帰セットです。
旧Build379チェックアウトが必要な「new geometry is opt-in」は対象外です。
代わりにBuild401とBuild402の第一章ダンジョンを同じ12乱数種で生成し、全体の一致を確認しました。

今回追加した12件
・中断した戦闘開始予約の読み込み復旧、HP／MP・現在地・鍵・討伐・資源の保持。
・実戦闘チェックポイントと第一章データの保護。
・複製された地域データ、非選択地域の予約の復旧と冪等性。
・トークン不一致で戦闘再開データを破棄した場合の予約解除。
・操作拒否、処理例外、保存false、保存例外の完全な復元。
・保存失敗後の古い探索データの書き戻し防止。
・実際の出発処理が1回で保存されることと失敗時の復元。
・第二章解禁と序章読了の表示境界。
・勇者加入→魔法陣研究→10連召喚→背景変更→回想→再読込の両立。
・勝利報酬の保存失敗→再試行→重複操作で付与が一度だけになること。
・探索の一時停止／非表示時の描画休止、再開、破棄。
・画像キャッシュの上限、最近使った画像の再利用、不正フレーム名の正規化。

既存テストの補正
Build376・380・391の戦闘再開用フィクスチャに、実際の戦闘中と同様のactiveBattleを追加しました。
保存予約だけの状態と、再開できる戦闘状態を区別するためです。
元のトークン保持・敗北再試行・報酬重複防止の期待値は維持しています。
予約だけの状態の復帰は新規Build402テストで別途確認しています。
Build394の実際のsaveBattleCheckpointによる再開テストも成功しています。

互換性・参照
旧セーブ6条件：Build401と版数以外一致。
第一章画面・回想20条件：HTMLと処理後の状態が一致。
第一章勇者12分岐、第二章11場面×3終了操作を既存回帰で確認。
第一章と第二章本編の既読保存関数は変更なし。
変更モジュール55件の構文、相対参照526件、版数付き参照を確認。
キャッシュ版数だけを更新した48モジュールは、クエリを除くとBuild401と一致。
CSS152件、CSS内画像等156参照：不明な参照0件。
第二章70シート・560モーション：実画像の寸法、切り抜き座標、256px描画領域を確認。
深淵・十神17体・136枚：8種の別画像、512px、透過、端と表示範囲を既存Build399テストで確認。
背景とサムネイル計12参照：ファイルのデコードと寸法を確認。

負荷計測
Node.jsコンテナーのCPU処理時間。実ブラウザーのFPSや操作時間ではありません。
キャラ3000体＋装備3000件、約5.3MBの模擬セーブ。回想モデル作成を各25回測定。
Build401中央値40.077ms → Build402中央値0.254ms。
同じ入力で回想モデルの内容が一致することも確認済みです。
ホーム生成中央値2.095ms、進行モデル中央値0.014ms。
Canvas用シートのアプリ側保持数を最大70から12へ制限。
シート画素だけのRGBA換算値は最大420.2MiBから72MiB相当です。
これは実測メモリーではありません。ブラウザー側キャッシュ、DOM画像、GPU等は別に存在します。

限界
Node VMの模擬DOM、ソースとHTML／CSS、画像デコード・座標による検査です。
実ブラウザー／iPhoneでの見た目、タッチ、FPS、メモリー使用量は未確認です。
