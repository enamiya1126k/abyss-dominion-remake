# build218 最小差し替え更新

- オンライン共闘の10階層ごとの階層ボスを、通常探索と同じボス定義・専用行動・属性・ステータス基準へ統一。100階層ごとの深淵、1000階層ごとの十神も同じ規則で登場します。
- マップ上の階層ボスを通常敵と区別し、接触時に専用確認画面を表示してから戦闘を開始します。
- 撃破後はボスを再出現させず、出口と「支配者の湯」を出現。参加者ごとの共闘貢献票を先に表示します。
- オンライン初回撃破後も、通常探索と同じ3択報酬を必ず保留・表示。過去にオンライン撃破だけ記録されて報酬を受け取れなかったセーブも、未受取分を復旧します。
- LINK BREAKを左上へ移し、階層属性・ボス名・ボス画像との重なりを回避しました。
- オンライン集会所の「レイド受付／ダンジョン門／闘技場／掲示板」を、透過ドット絵の施設素材へ置換。受付画面も黒・金・紫を基調に統一しました。
- レイド本体・幼体は既存の8フレーム素材を、待機・攻撃・被弾・撃破イベントに接続したまま維持しています。

Web版：`v2.11.53 build218`  
セーブキーとセーブスキーマ（58）は変更していません。

## 適用

ZIP内の相対パスを保ったまま、現行build217へ上書きしてください。サーバー側は `online-server/src/RoomStore.js` を上書き後、常設プロセスを再起動してください。
